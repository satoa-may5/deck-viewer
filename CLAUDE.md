# deck-viewer プロジェクト情報 (Claude向け)

このファイルは複数PC/複数セッション間でプロジェクトの文脈を引き継ぐためのもの。
git管理下に置き、どのPCでこのリポジトリを開いても同じ前提でやり取りできるようにする。
(Claude Codeのローカルメモリ機能は端末ごとに独立していて他PCに引き継がれないため、
永続化したい決定事項・方針は全てこのファイルに書く)

## このファイルの読み方

- **上半分は「現在の姿」**(構成 / データ・API / 運用ルール / 未着手項目 / 主要機能 /
  はまりどころ)。ここは実装を変えたら一緒に更新する。
- **下半分(`# 変更履歴・経緯`より下)は日付順の記録**。当時の判断とその理由が
  書いてあるが、現状と食い違う場合は上半分が正しい。撤回された判断には見出しの
  直後に ⚠ の注記がある。

## プロジェクトの目的

ローカルにあるカード画像でデッキを組み、一覧を表示・書き出しできるようにする。
対象はUnion Arena系のTCG。当初の目標は以下の3つで、いずれも実装済み:

1. カードプール単位で手持ちカードを登録する(画像アップロード→トリミング→情報入力)
2. 上にデッキ・下に手持ち一覧を並べ、クリックで足していくデッキ編集画面
3. ブラウザ・デスクトップ・モバイルのどれでも「ポチポチ操作するだけ」で使える
   (PWA + 単一exe配布)

その後、カード画像そのものを作れるオリカメーカーの統合、デッキ画像の書き出し、
印刷用ファイルの生成まで広がっている。

## 現在の構成(2026-08-21時点)

このセクションは**常に現状に合わせて更新する**。ここから下の日付付きセクションは
「そのとき何をなぜ決めたか」という経緯の記録で、現状と食い違う場合はこちらが正しい。

Node.js + Express のローカルサーバー + 素のJS(ビルド工程なし)。当初は無サーバーの
静的HTMLだったが、カード画像・メタデータを自動保存する必要があり切り替えた
(file://からの直接書き込みはブラウザの制限で困難、File System Access APIは
Chromium限定かつ不安定だったため)。配布は`pkg`による単一exe化 + PWA。

```
deck-viewer/
├── server.js              Express本体。静的配信 + 下記API。ASSETS_ROOT(読み取り専用の
│                          静的アセット)とAPP_ROOT(書き込みが要るdata/images。exe時は
│                          exeの隣)を分離している
├── package.json           依存: express, multer, adm-zip / scripts: start, dev, build:exe
├── data/                  ★中身は全てgit管理外(公開リポジトリのため。空なら自動生成)
│   ├── cards.json          カードのDB
│   ├── cardpools.json      カードプールのDB(配列の並び順=表示順)
│   ├── decks/{id}.json     デッキ1つにつき1ファイル
│   └── oricard-states/{cardId}.json  オリカ1枚分の「メーカーでの設定」(再編集用。
│                           イラストのbase64を含み大きいのでcards.jsonとは別管理)
├── images/                カード画像本体(git管理外)。ファイル名 = カードID + 拡張子
├── pool-exports/          配布ラインナップ。*.dvpool と index.json だけgit管理対象、
│                          生データの<名前>/フォルダは管理外
├── tools/                 package-release.js(exeビルド+zip化)、
│                          build-pool-export-index.js(index.json生成)
├── docs/dvpool-format.md  .dvpoolを手作り/スクリプト生成するための形式仕様
├── archive/               到達不能になった機能の退避先(カード情報の自動取得)
└── public/
    ├── index.html / js/home.js       ホーム(デッキ/カードプールの2タブ)
    ├── pool-detail.html / js/        カードプール個別ページ(カード一覧・追加/編集モーダル)
    ├── builder.html / js/builder.js  デッキ編集
    ├── deck-view.html / js/          デッキの画像出力・印刷
    ├── oricard/                      オリカメーカー(index.html + materials.js)
    ├── js/api.js                     サーバーAPIへのfetchラッパー
    ├── js/dialog.js                  showConfirm / showChoice / showToast / bindModalDismissal
    ├── js/card-render.js             カード1枚分のDOM生成(グリッド/ビルダー共通)
    ├── js/crop.js                    パン/ズーム式トリミング(CropToolクラス)
    ├── js/sortable.js                ポインタードラッグ並び替え(makeSortable、グリッド対応)
    ├── manifest.webmanifest, sw.js, icons/, js/register-sw.js  PWA
    └── css/style.css                 デザインシステム(CSS変数)
```

### 画面ごとの機能

**ホーム(`index.html`)**: 「デッキ」「カードプール」のフォルダタブ風2タブ(常に画面幅
半分ずつで固定)。両タブとも一覧表示/グリッド表示のトグル(既定はグリッド、
localStorageで記憶)、先頭の点線ゴースト行からの作成、ドラッグ並び替え、★お気に入り
(と「お気に入りのみ表示」フィルター)、リネーム、削除。デッキはさらに複製、
カードプールはさらにインポート/エクスポート。起動時にGitHubの最新Releaseと自分の
バージョンを比べ、新しければ上部にバナーを出す(押すとReleaseページを開くだけ)。

**カードプール個別ページ(`pool-detail.html`)**: プール名の編集、カード一覧
(一覧/グリッド切り替え、ドラッグ並び替え、絞り込みサイドバー)、右下の＋ボタンから
「カードを1枚追加する」「カードを複数枚追加する」「オリカを追加する」の3択。
カード画像をクリックすると編集モーダル(オリカなら「オリカメーカーで編集」も出る)。
「選択して削除」で一括削除。右側にサムネイル設定とカードスタイル(オリカの見た目を
プール単位で揃える設定)。

**デッキ編集(`builder.html`)**: 上がデッキ、下が手持ちカード(参照プールを複数選択)。
左クリックで1枚追加・右クリックで1枚削除(両ペイン共通)。undo/redo(Ctrl+Z/Y、最大30件)、
必要エナジー昇順ソート、絞り込みサイドバー、☰メニュー(サムネイル設定・統計情報・
デッキのエクスポート/インポート)。統計情報には必要エナジー分布・トリガー内訳の
グラフと、初手7枚+マリガンの成功率を厳密計算するマリガンシミュレーターがある。

**画像出力・印刷(`deck-view.html`)**: 16:9のデッキ一覧画像の書き出しと、A4に9枚ずつ
実寸で並べた印刷用ファイル(TIFF/PNG)の生成。印刷サイズのキャリブレーション付き。
詳細は後述の「デッキの画像出力・印刷」セクション。

**オリカメーカー(`public/oricard/`)**: カード画像そのものを合成生成するツール。
別プロジェクト(UA-maker)として作られたものをiframeで埋め込んでいる。

### この形になった経緯で、今も効いている決定

- **`add-card.html`は廃止済み**。カード登録は必ず`pool-detail.html`のモーダル経由。
  古いリンクが残っていないか変更時は注意する。
- 画像選択後のトリミングは、モーダルの上に重なる**入れ子のポップアップ**
  (`#crop-popup`)で行う。キャンセルすると直前に確定していた画像がそのまま残る。
- カード名が空欄なら`CARD-001`形式の連番を**実際に採番して保存する**(表示専用の
  フォールバックではなくDBに実名が入る)。入力欄のプレースホルダーに次の番号を出す。
- カードプールの削除は**カスケード削除**(プール内のカードレコードと画像も消える)。
  デッキ側に残った参照は「見つからないカード」としてグレースフルに扱う。
- 確認ダイアログはブラウザ標準の`confirm()`ではなく`js/dialog.js`を必ず使う。

## データ/API仕様

### ID採番

pool/card/deck いずれも`uniqueId(prefix, takenIds)`で採番する(プロセス内の単調増加
カウンタ + 既存IDとの突き合わせの二重)。**カードIDは画像ファイル名も兼ねる**ため、
衝突すると別のカードの画像を黙って上書きしてしまう。`Date.now()`だけの採番は
「複数枚追加」の連続POSTで実際に潰れるので戻さないこと。URLパラメータをファイル名に
使う箇所(`deckFile(id)`など)は必ず`ID_PATTERN`の検証を通す(パストラバーサル対策)。

### カード(`data/cards.json`)

各カードは必ず1つの`poolId`に属する。フィールド:

| キー | 内容 |
|---|---|
| `id` | `card-...`。画像ファイル名にもなる |
| `name` | 識別コード。空欄で保存すると`CARD-001`形式の連番が実際に入る |
| `cardName` | 実際のカード名(manifestの`name`はここに入る)。表示は`cardName || name` |
| `cost` / `ap` | 必要エナジー / 消費AP(数値 or null) |
| `color` / `rarity` / `bp` / `generatedEnergy` | 自由文字列 |
| `type` | `character` / `event` / `field` のみ(許可リスト外は空文字に丸める) |
| `trigger` | `active`/`drow`/`final`/`get`/`raid`/`special`/`color` または空(トリガーなし) |
| `parallel` | パラレルかどうか(真偽値) |
| `attribute` | 特徴(文字列の配列) |
| `effect` | 効果テキスト(複数行) |
| `unedited` | 「複数枚追加」で作られ、まだ個別編集されていない印。`PATCH`を通すとfalseになる |
| `imageExt` / `imageUpdatedAt` | 画像の拡張子と最終差し替え時刻(URLの`?v=`に使いキャッシュを外す) |
| `order` | プール内の表示順 |

画像形式はpng/jpegのみ受け付ける(`MIME_TO_EXT`)。アスペクト比は縦88:横63固定
(CSSは`aspect-ratio: 63 / 88`、手動トリミングの出力は630×880)。

`cards.json`に残っている`infoUncertain`は、削除した自動取得機能の名残で今は読まれない
(既存データを書き換えないため消していないだけ)。

### カードプール / デッキ

- カードプールは単なるグループ分けで、**配列の並び順がそのまま表示順**。
  `favorite` / `thumbnailCardId` / `oricardStyle`(オリカの見た目をプール単位で
  揃える設定)を持つ。
- デッキは`poolIds`(参照するプールIDの配列)・`cards`(`{cardId,count}`)・`order`・
  `favorite` / `thumbnailCardId`を持つ。
- 名前は`uniqueName()`で重複を避け、衝突すると` (1)`, ` (2)`...が付く。
- デッキのリネーム・複製に専用APIは無く、`POST /api/decks`(id指定でupsert)を
  フロント側で使い回している。

### API一覧

| メソッド・パス | 備考 |
|---|---|
| `GET /api/version` / `GET /api/latest-release` | 手元のバージョン / GitHubの最新Release(1時間キャッシュ) |
| `GET/POST/PATCH/DELETE /api/pools(/:id)` | PATCHは`name`/`favorite`/`thumbnailCardId`/`oricardStyle`を部分更新。DELETEは**カスケード**(所属カードのレコードと画像も消す) |
| `POST /api/pools/reorder` | `{order:[id,...]}` |
| `GET /api/pools/:id/export-zip` / `POST /api/pools/import-zip` | `.dvpool`の書き出し/取り込み |
| `GET /api/github-pools` / `POST /api/github-pools/import` | 配布ラインナップの一覧(`pool-exports/index.json`をrawから取得、60秒TTL)とダウンロード取り込み |
| `GET/POST/PATCH/DELETE /api/cards(/:id)` | GETは`?poolId=`でフィルタ可。DELETEは画像ファイルも消す |
| `POST /api/cards/:id/image` | multipart。既存画像を差し替え、`imageExt`/`imageUpdatedAt`を更新 |
| `POST /api/cards/reorder` | 渡したidだけ`order`を0始まりで振り直す(通常は同一プール分だけ渡す) |
| `GET/PUT /api/cards/:id/oricard-state` | オリカの再編集用の設定。`data/oricard-states/{cardId}.json` |
| `GET/POST/PATCH/DELETE /api/decks(/:id)` | PATCHは`favorite`のみ。POSTはid指定でupsert |
| `POST /api/decks/reorder` | `{order:[id,...]}` |
| `GET /api/decks/:id/export-zip` / `POST /api/decks/import-zip` | `.dvdeck`。インポートは参照カードごと新規プールを作って取り込む |

アップロードのMulterインスタンスは用途ごとに分ける(カード画像15MB用の`upload`と、
zip 500MB用の`uploadZip`)。**同じインスタンスを使い回すと上限が共有されて静かに
失敗する**(実際に63MBの`.dvpool`が15MB制限に引っかかった)。Multerのエラーを
JSONで返すエラーハンドラを`app.listen`の直前に置いてある。

## 決定事項・方針

- **2026-07-22時点の最新方針**: ユーザーから「コミットプッシュはこれから勝手にしていいよ
  (一応報告して)」と明示的な標準許可をもらった。これ以降は、都度確認を挟まずcommit/push
  してよい。ただし実行した際は必ずその旨をユーザーに報告すること(何をコミットしたか
  一言添える)。それ以前の「基本ユーザー自身が行う」という方針は本プロジェクトについては
  上書き済み。**同日中に追加の指示**: 「コミットプッシュはある程度変更が溜まってから
  でいい」とのこと。細かい修正1つ1つでコミットせず、ある程度まとまった単位(1つの
  依頼・1つの機能追加が完了した区切りなど)でまとめてcommit/pushする。
- **カード情報もgit管理外**(2026-08-18に変更、それ以前の記録は古い)。
  `images/*`に加えて`data/cards.json`・`data/cardpools.json`・`data/decks/*`・
  `data/oricard-states/*`を`.gitignore`で除外している(公開リポジトリなので個人の
  使用データを載せないため)。**PC間のデータ移行はgitではなく`.dvpool`/`.dvdeck`の
  エクスポート/インポートで行う**(画像も一緒に入る)。`git clone`直後はカードプールも
  カードも0件で、`server.js`が空のファイルを自動生成する。
- 命名規則: カードIDとファイル名を一致させる(マッピング処理を作らずに済む)。
  IDはサーバー側で自動採番する(上記データ/API仕様参照)。


## 「ビルド・リリース更新は勝手にしていい」の標準許可(2026-08-19 → 2026-08-21に拡大)

コミット/プッシュに続き、2026-08-19に「ビルドとかも勝手にしてね」という標準許可を
得た。当初は**ビルドのみ**の許可で、GitHub Releaseの作成・公開は都度確認する運用
だったが、**2026-08-21に「ビルドとリリース更新は毎回勝手にやっていい」と明示的に
拡大された**ため、以降は`npm run build:exe`とリリースのzip差し替え・リリースノート
更新まで、都度確認を挟まず実行してよい(実行後にその旨を報告する運用は継続)。
なお「新しいバージョン番号でリリースを新規作成する」ケースは、バージョンを上げる
判断自体がユーザーのものなので、そこだけは一言確認するのが無難。ビルド前には毎回、同じ
バージョンのexeが起動中でないか(`Get-Process | Where-Object ProcessName -like
"*deck-viewer*"`)を確認すること(ロックされていると`EPERM`で失敗する、上記
「exeビルドの運用ルール」参照)。

## exeビルドの運用ルール(2026-08-19、v1.1.0公開後の混乱を受けて整理)

v1.1.0のビルド作業で「同じファイル名`deck-viewer-<version>.exe`だが中身が違う2種類の
ビルド(公開用/ローカル用)」を作り分ける必要があることが判明し、実際に取り違えて
「オリカメーカーが見つかりません」という誤報告を招いた。以降のビルド手順を明文化する:

1. **同じバージョンのexeが既に起動していないか確認してから`npm run build:exe`する**。
   起動中だと`dist/deck-viewer-<version>`フォルダのロックで`rmSync`が`EPERM`を出して
   失敗する(`Get-NetTCPConnection -LocalPort 3000 -State Listen`→`Get-Process`で
   犯人のPIDを特定して停止する)。**2026-08-21に「exe起動中だったら勝手に消していい」
   という明示的な許可を得たので、ユーザー自身が使っている可能性があるプロセスでも
   確認せず停止してよい**(それ以前の「疑わしい場合は先に確認する」という運用は撤回)。
2. ~~配布用と自分専用で`public/oricard/`を退避し分ける~~ → **この手順は廃止**。
   オリカメーカーは常に同梱する方針に変更したため(後述の「ローカル限定方針を撤回」
   参照)、配布用・自分専用の区別なく、そのまま`npm run build:exe`すればよい。
   同じファイル名で中身が違うexeが生まれる危険もこれで無くなった。
   なお`data/`・`images/`(開発者自身の実カードデータ)は
   `tools/package-release.js`がビルド中だけ`.build-stash/`へ自動退避し、
   成否にかかわらず`finally`で元に戻すので、手動での退避は不要。
3. **ビルド後の検証は、配布zipの中身一覧(`unzip -l`)では絶対に確認しないこと**
   (zipにはexe本体しか入っておらず、pkgがexe内部に埋め込むスナップショットの中身は
   zip一覧からは一切見えない。過去に実際にこの誤ったチェック方法で「除外できている」
   と誤認し、後から矛盾する状況を生んだ)。**正しい確認方法は、実際にビルドしたexeを
   別フォルダに展開して起動し、`curl http://localhost:<port>/oricard/index.html`で
   200(同梱されている)か404(除外されている)かをHTTPレスポンスで直接確認すること**。
4. GitHub Releaseにアップロードするのは`tools/package-release.js`が作る
   `dist/deck-viewer-<version>.zip`。

## 現在の未着手・保留項目(2026-08-21時点)

**現在、ユーザーから依頼されて未着手のままになっている項目は無い。**
新しく保留になったものはここに書き足すこと。

### やらないと決めたこと

- **LAN経由でのPWAインストール(2026-08-21に「やらなくていい」で決着)**:
  スマホからPCのLAN IPを開いてホーム画面に追加する、という話。平文httpのLAN IPは
  「secure context」にならず`navigator.serviceWorker`自体が生えないため、
  アプリ側の変更だけでは原理的に不可能(ブラウザの仕様)。**Chromeは証明書エラーの
  あるページではSW登録を拒否するので、自己署名証明書を出して警告を無視してもらう
  という逃げ道も無い**。実現するには (A) 端末にローカルCAを入れる[警告が出続ける]、
  (B) Cloudflare Tunnel等でインターネットに出す[ローカル専用アプリの前提が崩れる]
  のどちらかが必要で、どちらも割に合わないと判断した。
  **今後この話を蒸し返さないこと**。同一Wi-Fi内でブラウザのタブとして使えれば十分、
  という位置づけで確定。調査の詳細は下の「モバイルからのLANアクセス」セクション参照。


## 外部にあるファイルとの同期

- **`C:\Users\user\Desktop\UA-maker\index.html`(オリカメーカーのスタンドアロン原本)**:
  `public/oricard/index.html`と同じ内容に保つ運用。差分は**スタンドアロン版だけの
  ブロック(`#standaloneIO`のPNG/JSONボタンと、その配線+`saveStyleBtn`を隠す
  初期化コード)だけ**で、双方のファイル内にその旨のコメントがある。
  統合版を更新したらこちらにも反映し、上記ブロックを足し直すこと
  (2026-08-21時点で同期済み。差分はそのブロックの23行のみ)。
- **`C:\Users\user\Desktop\UA-maker\materials\`(素材の原本)**: ここに素材PNGを
  足したら、`oricard-source/HANDOFF.md`記載のPowerShellコマンドで`materials.js`を
  再生成し、`public/oricard/materials.js`にコピーし直す(自動化されていない手動手順)。
- **`pool-exports/*.dvpool`**: 追加・更新したら必ず
  `node tools/build-pool-export-index.js`で`index.json`を作り直してから一緒に
  commit/pushする。忘れると新しい`.dvpool`がインポート画面に出てこない。

## デッキの画像出力・印刷(`deck-view.html`、2026-08-21時点)

このファイルに長らく記録が無かったが、`deck-view.html`はもう「読み取り専用の
グリッド表示」ではなく、**デッキの画像出力と印刷用ファイル生成の画面**になっている
(デッキ編集画面の「画像を出力」から遷移する)。

### 画像として保存

デッキ一覧を16:9(1600×900)のcanvasに描いて書き出す。デッキ名・カード名・マナカーブ
の表示切り替え、最終行の寄せ方(中央揃え/左詰め)、canvas上でのドラッグ並び替えに
対応する。表示設定は`localStorage`(`deck-viewer-export-settings`)に保存される。
レイアウトは`computeCardLayout()`が「63:88のカードが一番大きくなる列数」を総当たりで
選ぶ方式。

### 印刷用ファイル

A4に3×3の9枚ずつ、実寸(63mm×88mm)で並べる。切り出してプロキシにする用途。

- **台紙のピクセル数は「カードが何ピクセルで刷られてほしいか」から逆算する**
  (`printGeometry(cardWPx)`)。元画像より大きく引き伸ばしても情報は増えず、
  ファイルサイズだけ膨らむため、DPIを先に決める方式をやめた。
  - 通常サイズ: カード600×838px → 台紙2000×2829px(約242 DPI)
  - 高画質: カード1200×1676px → 台紙4000×5657px(約484 DPI)
- 高画質は**デッキに2倍解像度のオリカ(幅900px以上)が含まれるときだけ**選択肢を出し、
  その場合は既定にする。600px幅のカードしか無いなら引き伸ばしにしかならない。
- 形式はTIFF(無圧縮・既定)とPNG(可逆圧縮)。どちらも1ページ1ファイル。
  TIFFはブラウザが吐けないので`encodeTiff()`で手書きしている(無圧縮8bit RGB、
  単一ストリップ)。PNGは`canvas.toBlob()`任せだが、解像度(pHYs)チャンクだけは
  付けてくれないので`withPngDpi()`でIHDRの直後に挿し込んでいる。
- 推定サイズを出し、10MB超で「コンビニのネットプリントが使えないかもしれない」
  警告を赤字で出す。
- TIFF選択時は「対応していないサービスもある」という**注記**(赤字の警告ではない)を
  出す。当初「TIFFはほとんどのサービスが受け付けない」と書いていたが、
  **2026-08-21にユーザーから「セブンのネットプリントはTIFFいける」と指摘を受けて
  訂正した**。断定せず、分からなければPNGが確実、という書き方にしてある。

一度PDF出力(FlateDecode + PNG Predictorの自前ライター)も実装したが、「普通にPNGで
よくない?」という指摘を受けて削除した。PDFの利点は「ページに実寸(pt)を持てる」ことと
「同じカードを1回だけ埋め込んで使い回せる」ことだったが、どのみち印刷側で用紙に
合わせて拡大されるため下のキャリブレーションで吸収でき、実用上の差が小さかった。

### 印刷サイズのキャリブレーション

プリンター/ドライバの設定で実際の印刷サイズはずれる。100mmの横線・縦線を入れた
調整用シートを印刷して定規で実測し、その値から倍率を出して以降の印刷を補正する。
**補正値は形式ごとに分けて保存する**(`{png:{scaleX,scaleY}, tiff:{...}}`。ずれ方は
解像度タグ(TIFF)やpHYs(PNG)の解釈の違いで変わるため)。**画質の設定では紙の上の
寸法は変わらないので、そちらでは分けていない。**

### カードの実サイズについて(837と838の話)

過去に「オリカメーカーの下1行を削って600×837にした」という経緯がある(素材の最下行が
透明で、背景イラストが1pxはみ出して見えたため)。現状の実データは:

| | サイズ | 63:88からのズレ |
|---|---|---|
| 手動トリミング | 630×880 | ±0% |
| `.dvpool`の実カード | 600×838(多数派) / 600×837 / 600×836 | +0.011% / +0.131% |
| オリカ(2倍) | **1200×1674** | +0.131% |

600幅なら理論上の高さは838.095なので、**印刷側は838(と2倍の1676)を採用**している。
837との差は88mm換算で0.115mmしかなく、プリンターの精度もカッターの精度も下回る。
オリカメーカー側の837は「見える不具合を消すための対処」、印刷側の838は「紙の上で
正しい寸法にするための値」で目的が別なので、**メーカー側を838に戻す必要はない**。

## オリカメーカーの機能追加(2026-08-19〜21)

### カードスタイル(プール単位で見た目を揃える)

カード名イラスト・枠の色/明度・背景イラスト・テキストエリアイラストを、カードプールの
`oricardStyle`として保存できる。次に同じプールでメーカーを開くと自動的に適用される。
`pool-detail.html`の右側にプレビュー付きで表示され、そこから**スタイルに含まれる項目
だけ**を編集する専用画面(`setStyleOnlyMode`)を開ける。

- 新規オリカ追加時にスタイルが変わっていたら、「このカードスタイルを保存しますか?」を
  3択(保存しない / 保存する / 保存して全てのオリカに適用する)で聞く。何を選んでも
  カード自体は追加される。
- 「全てのオリカに適用する」は**実際にプール内の既存オリカを1枚ずつ再描画する**
  (オフスクリーンiframeでメーカーを開き、保存済みの`oricard-state`にスタイルを
  被せて描き直し、画像を差し替える)。最初はフラグを立てるだけの実装にしてしまい、
  「全然生きてないけど」と指摘された。
- 再描画したカードが**リロードしないと見た目に反映されない**問題があったため、
  `card.imageUpdatedAt`を更新して画像URLに`?v=`を付けるようにした。

### オリカの再編集

「カードを編集」モーダルの「オリカメーカーで編集」から、作成時の設定ごと開き直せる。
設定は`data/oricard-states/{cardId}.json`に1枚ずつ保存する(イラストのbase64を含み
大きいので、`cards.json`には入れない)。

### SRテンプレート

下地を`Base_image_SR.png`に差し替えるテンプレート。カードイラスト2が枠内(524×640)、
カードイラスト1がカード全面(ただしテキスト類の下)、テキストエリアイラストは無し。
効果テキストは最大12行・**下揃え**で、下端はトリガーの有無とバナーの高さ
(ファイナルだけ2行ぶん)で変わる。Typesバッジもテキスト最上行のすぐ上に付いてくる。

### 2倍解像度での描画

`RENDER_SCALE=2`で常に2倍(1200×1674)で書き出す(印刷時の粗さを減らすため。
設定項目は作っていない)。レイアウト定数は600×838座標系のまま、
`ctx.setTransform(RENDER_SCALE,...)`で拡大している。

### イラストのサイズテンプレートDL

各イラストスロットの見出し横のボタンから、「この大きさで作ればぴったり入る」枠を
PNGで落とせる(`illustSlotSpec()`/`downloadIllustTemplate()`)。マスク素材の実形状を
なぞって出力する。

### 効果テキストのトークン変換

メーカー内は`[[touzyouzi]]`のような独自記法のまま保持し、**カードプールに登録する
時点で**他のカードと同じ表記(`[登場時]`)に変換する(`ORICARD_EFFECT_TOKEN_TEXT`/
`oricardEffectToText()`、`public/js/pool-detail.js`)。対応表は`pool-exports/*.dvpool`
全55個の効果テキスト実データ(約9000件)を集計して決めた(**丸括弧は全角、数字と
`+`は半角**)。`[[energy]]`は連続した個数がそのまま数字になる(2個並べれば`2`)。
未知のトークンは変換せず残す(素材を足したときに気付けるように)。
## はまりどころ(実装時に踏んだバグ、再発防止用)

- **Service WorkerのシェルキャッシュがJS/CSSの変更を握り潰す**: `sw.js`は
  `CACHE_NAME`(例: `deck-viewer-shell-v2`)をキーにshellファイル(html/css/js)を
  cache-firstで返す。`public/`配下のJS/CSS/HTMLを変更したら**必ず`sw.js`の
  `CACHE_NAME`をインクリメントし、`SHELL_FILES`に新規ファイルがあれば追加すること**。
  やらないと、ブラウザは古いsw.jsのバイト列のままだと判定して再インストールせず、
  変更が永久に反映されない(実際に`Api.createPool is not a function`という形で
  発生した)。開発中に動作確認する際は、コンソールで
  `navigator.serviceWorker.getRegistrations().then(rs=>rs.forEach(r=>r.unregister()))`
  `caches.keys().then(ks=>ks.forEach(k=>caches.delete(k)))` を実行してから
  リロードすると確実に最新化できる。
- **`hidden`属性とCSSの優先順位**: `.pool-chip { display: flex; }` のような
  クラスセレクタは、UAスタイルシートの `[hidden] { display: none }` と同じ詳細度
  (0-1-0)だが、author stylesheet(自分の`style.css`)の方がUAスタイルシートより
  優先されるため、`hidden`属性を上書きしてしまい要素が消えなくなるバグがあった。
  対策として`style.css`冒頭に `[hidden] { display: none !important; }` を追加済み。
  今後 `hidden` 属性で出し分けする要素に無条件の `display` ルールを書くときは注意。
- **共通コンポーネントのコールバックをハードコードしない**: `home.js`の`startRename()`は
  デッキ行/カードプール行の両方から共有される「その場でリネーム」UIだが、初期実装では
  キャンセルボタンのハンドラを`renderDecks`に決め打ちしていたため、カードプール側で
  リネームをキャンセルしても画面が更新されず固まって見えるバグがあった。共有UIヘルパーを
  複数の呼び出し元で使う場合、再描画関数などのコールバックは必ず引数として受け取り、
  呼び出し元ごとに正しい関数を渡すこと。
- **ドラッグ中の要素にCSS transitionが付いていると`getBoundingClientRect()`が
  古い値を返す**: `js/sortable.js`(`makeSortable`)はドラッグ中の要素に
  `style.transform = translateY(...)`を設定し、直後に`getBoundingClientRect()`で
  現在位置を読んで並び替え判定をしているが、`.deck-row`に`transition: transform 0.2s`
  が付いていたため、transformを設定した直後(同一の同期処理内)に読み取ると
  アニメーション開始前の**古い位置**が返ってきてしまい、ドラッグの並び替え判定が
  一切発動しないバグがあった。対策として`.dragging`クラスに
  `transition: none !important;`を追加し、ドラッグ中は該当要素のtransitionを
  無効化して即座に新しい位置が反映されるようにした。今後、transformで要素を動かして
  すぐに`getBoundingClientRect()`を読む処理を書く場合、その要素(または祖先)に
  transformのtransitionが付いていないか必ず確認すること。
- **ドラッグの上下限クランプと入れ替え判定の境界条件**: `js/sortable.js`にドラッグ範囲を
  リストの先頭〜末尾内に収めるクランプ処理を追加した際、クランプ後の位置が隣接要素の
  中央と**ちょうど一致**するケース(要素が2個しかない時にリストの端まで一気にドラッグする
  場合など)で、入れ替え判定が厳密不等号(`<`/`>`)だったため境界上で入れ替えが
  発火しないバグがあった。`<=`/`>=`に変更して解決済み。座標比較で「ちょうど一致」が
  起こりうる場面(クランプ後の値を使う場合など)では厳密不等号を避けること。
- **ドラッグ並び替えで`getBoundingClientRect()`を使うと表示されない要素の位置ズレで
  暴走する**: `js/sortable.js`の入れ替え判定は当初、非ドラッグ要素(隣接要素)の位置も
  `getBoundingClientRect()`で読んでいたが、入れ替え時に付与するFLIPアニメーション
  (`animateDisplacement`、隣接要素をtranslateYで一瞬ズラしてから0に戻すことで
  「スライドして移動した」ように見せる演出)の**アニメーション実行中に別のpointermove
  イベントが来ると、まだアニメーション中で本来の位置に落ち着いていない隣接要素の
  transformされた位置を読んでしまい**、入れ替え判定が誤発火して同じ2要素の間を
  行ったり来たりする(swapしてもすぐ元に戻る)現象が発生した。実機での手動ドラッグ
  (`left_click_drag`)でのみ再現し、合成した単発の`PointerEvent`ディスパッチでは
  再現しなかった(移動距離が小さすぎて閾値を超えなかったり、連続moveイベントの
  タイミングが違ったりするため)。対策として、入れ替え判定用の座標取得を
  `getBoundingClientRect()`から`offsetTop`/`offsetHeight`(レイアウト上の位置。
  CSS transformの影響を受けない)に変更し、解決した。**ドラッグ中の要素にCSSの
  transformでアニメーションを掛ける場合、その位置を「判定」に使う他のコードは
  必ずtransform非依存の値(offsetTop等)を使うこと。realなユーザー操作でしか
  再現しないバグもあるので、`left_click_drag`のような実際のポインタ操作での検証も
  怠らないこと(合成イベントのディスパッチだけでは不十分な場合がある)。**
- **画像を上書きするAPI(`POST /api/cards/:id/image`など)の動作確認は、必ず使い捨ての
  テスト用カードに対して行うこと**: `curl`で直接叩いて動作確認した際、手近にあった
  ユーザーの実カード(プール「a」の1枚)を対象にしてしまい、画像ファイルを別カードの
  画像で上書きしてしまった(バックアップなし、復元不可)。名前などのメタデータは
  無事だったが画像本体は失われた。今後、既存データを書き換える可能性のあるAPIを
  動作確認する際は、必ずその場で作成した使い捨てのプール/カードを対象にし、
  確認後に削除すること。ユーザーの既存データには絶対に触れない。
- **`js/sortable.js`をグリッド(2次元)対応に汎用化する際、「常に一番近いアイテムと
  スワップする」ロジックだと少数アイテム(特に2個)で行ったり来たりの振動バグに
  なる**: 当初は「ドラッグ中心に最も近い他アイテム」を無条件にスワップ対象にして
  いたが、2個しかないグリッドで大きくドラッグすると、1回スワップした後さらに
  同方向に動かしただけで「唯一残っている他アイテム」が常に最近傍判定されてしまい、
  逆方向にスワップし戻ってしまう(結果的に何もドラッグしていないのと同じ見た目に
  なる)バグがあった。修正として、「自分自身の現在のスロットとの距離」を基準値に
  设定し、**それより明確に近い場合のみ**他アイテムをスワップ対象とする方式に変更
  した(基本的には中点を跨いだかどうかの判定と同じだが、1軸限定ではなく2次元
  グリッドやアイテム数に依存せず機能する)。また、ドラッグ中の見た目の位置は
  DOMの実位置ではなく「ポインタ移動を開始からずっと積算し続ける仮想位置」で追跡し、
  スワップでDOM上の位置が変わった直後も仮想位置ベースで transform を再計算する
  ことで、スワップの瞬間に見た目が飛ばない/振動しないようにしている。同じ
  コンテナに複数の`makeSortable()`(一覧表示の`.card-row`用とグリッド表示の
  `.card-item`用)を登録しても、`itemSelector`/`handleSelector`が異なる限り
  互いに干渉しない(該当しない方は`e.target.closest(handleSelector)`がnullになり
  即return するため)。
- **画像/カードのクリックとドラッグを同じ要素(グリッドの画像本体など、専用の
  ドラッグハンドルがない場所)で共存させるには、`pointerdown`直後に
  `preventDefault()`してはいけない**: 一定距離(`DRAG_THRESHOLD`)動くまでは
  「クリックの可能性がある」として何もせず、閾値を超えて初めてドラッグ開始
  (`preventDefault()`・`dragging`クラス付与)する設計にした。これにより、
  閾値未満の操作は通常の`click`イベントとして素通りし、編集モーダルを開く/
  選択モードのチェックボックスを切り替える、といった既存のクリックハンドラが
  そのまま動作する。
- 実装中に`read_console_messages`ツールが、ページを再読み込みしても古い
  `console.log`の内容を返し続ける(実際には新しいJSが読み込まれていて該当の
  `console.log`自体が消えているのに、ツールの出力だけ古いまま)という挙動に
  遭遇した。コード変更後の検証で`console.log`デバッグに頼ると誤った結論に
  至る可能性があるため、確証が欲しい場合は`window`に直接値を書き込んで
  `javascript_tool`で読み出す・`fetch()`で配信されているファイルの内容を
  直接確認する、といった手段と併用すること。
- **ネイティブ画像ドラッグの抑止は、画像を使っている箇所全てに個別に必要**:
  前回`.card-row-thumb img`/`.card-frame img`には`draggable=false`を設定したが、
  `js/crop.js`のトリミングツールが内部で生成する`<img>`要素には設定し忘れており、
  トリミングポップアップ内でパン操作をしようとすると半透明のゴースト画像が
  カーソルに追従するバグが残っていた(`CropTool`のコンストラクタで
  `this.img.draggable = false;`を追加し、CSSにも`-webkit-user-drag: none;`を
  追加して解消)。今後も新しく`<img>`を動的生成する箇所を追加する際は、
  忘れずにこの対策を入れること。
- Claude Browserペインの`left_click_drag`が、環境によってはドラッグ操作として
  ブラウザに認識されない(pointerdown/move/upが発火しない)ことがあった一方、
  `PointerEvent`をJSで直接dispatchする方法(pointerdown→pointermove→pointerup、
  同一`pointerId`)は確実に`sortable.js`のドラッグ判定を発火させられた。
  `left_click_drag`で並び替えが反応しない場合は、まずJSでの合成イベントdispatchで
  ロジック自体の健全性を切り分けると原因の切り分けが早い。
- **`Element.releasePointerCapture()`が例外を投げることがある**: 実際のポインタ操作
  (`left_click_drag`)による`pointerup`では、ブラウザがハンドラ実行前に暗黙的に
  ポインタキャプチャを解放している場合があり、その後`releasePointerCapture()`を
  呼ぶと例外を投げて以降の処理(並び替え結果の確定・保存)が実行されないことがあった。
  `try/catch`で無害化して以降の処理を必ず実行させるようにした。

- **Claude Browserペイン(自動テストツール)は、ペインが実際に表示されていないと
  フレームを一切描画・合成しない**: `computer{action:"screenshot"}`は
  「the page is not compositing frames」で必ず失敗する状態があり、その状態では
  `element.getAnimations()[0].currentTime`が実時間が経過しても0のまま固まり、
  **`requestAnimationFrame`のコールバックは一切呼ばれない**(直接カウンターを
  仕込んで検証済み、2秒待っても0回)。一方`setTimeout`は正常に動作する。これにより、
  「CSSアニメーションが動いているか」をこのツールのgetComputedStyle/getAnimations経由で
  検証しようとすると、実際には正しく動くコードでも「動いていない」ように見えてしまう
  (このセッションで実際に数回この罠にハマり、ユーザーからの「まだ見えない/点滅しない」
  という実機報告を頼りに原因を切り分け直す羽目になった)。**「予定通りの見た目か」を
  本当に検証したい場合、computed styleの確認だけで安心せず、`requestAnimationFrame`に
  依存する処理は`setTimeout`に置き換えるなどして、この制約を回避できないか常に疑うこと**。
- **SVGで単純な線アイコン(ハンバーガーメニューの三本線など)を描画して「見えない」と
  報告された場合、原因を深追いするより先にSVG自体をやめてしまうのが早い**: 今回、
  (1) `<g stroke="currentColor">`で親に色を設定し子`<line>`に継承させる方式、
  (2) 各`<line>`に直接CSSクラスで`stroke`を指定する方式、の2つを試したが
  どちらも実機で「見えない」と報告された(原因はcomputed styleの確認だけでは
  特定できなかった)。最終的に`<span>`(width/height/background指定の単純な矩形)
  3本に置き換えたところ解決した。単純な矩形アイコンなら、SVGよりCSSボックスの方が
  レンダリング経路がシンプルで確実。
- **`<button>`要素のデフォルトpaddingは左右非対称な場合があり、明示的に`padding:0`を
  指定しないと、中身のアイコンが微妙に偏って見える**: 上記の三本線アイコンで、
  1本目だけ太く/左寄りに見えるという報告があった。原因は(a)ボタンのデフォルト
  paddingがリセットされていなかったこと、(b)`justify-content: space-between`による
  等間隔配置が3要素とも同じ幅になる保証がなかったこと、の2つ。`padding:0`+
  `gap`+`flex: 0 0 <固定値>`の組み合わせに変えて解決した。
- **既存のボタンをドロップダウンメニューの項目として作り変える際、そのボタンの
  IDセレクタを使った古いCSSルールが残っていないか確認すること**: `#deck-thumbnail-mode-btn`
  というIDは、単独の目立つボタンだった頃の`.active`スタイル(オレンジ背景)が
  `style.css`に残っており、新しく追加したクラスベースの赤色スタイルをIDセレクタの
  詳細度で上書きしてしまっていた。
- **Multerの`fileSize`上限は、同じ`multer()`インスタンスを使い回す全てのルートで
  共有される**: カード画像1枚(15MB上限)用に作ったインスタンスを、まるごと別の
  ファイルプール(zipファイルなど、実際には数十MBになりうる)のアップロードにも
  使い回すと、想定より小さい上限で静かに失敗する。用途ごとに異なる上限が必要な
  アップロードは、別々の`multer()`インスタンスを用意すること。また、Multerの
  エラーをキャッチするExpressのエラーハンドリングミドルウェア
  (`(err, req, res, next) => {...}`、4引数)を用意しておかないと、エラー時に
  ExpressのデフォルトHTMLエラーページが返ってしまい、JSON前提のクライアントコードが
  `res.json()`で「Unexpected token '<'」という分かりにくいエラーを出す。
- **グローバルスコープの変数名は、`history`・`location`・`navigator`など既存の
  ブラウザ組み込みオブジェクト名と衝突しないよう注意する**: undo/redo機能の実装で
  変数名を`history`にしてしまい、`window.history.replaceState(...)`の呼び出しが
  ローカル変数(ただの配列)のメソッド呼び出しとして解釈されてしまい、
  「history.replaceState is not a function」という一見無関係に見えるエラーになった。

---

# 変更履歴・経緯(以下は日付順の記録)

ここから下は「そのとき何をなぜ決めたか」の記録。上のセクションと食い違う場合は
上が正しい。撤回された判断には見出しの直後に ⚠ の注記を付けてある。

## 公開状況(2026-07-24)

> **⚠ この時点の「privateに戻した」という記述は撤回済み。**
> 2026-08-02にリポジトリを再びpublicにし、現在もpublicのまま。
> 後述の「リポジトリを再度public化し〜」を参照。以下は当時の記録。

友人に試してもらう目的で一度GitHubリポジトリを**private→public**に変更したが、
「gitコマンドを打たせるのではなく、DLしてダブルクリックするだけで動くフリーソフト
のような形にしたい」という要望を受け、**リポジトリは再びprivateに戻した**(公開は
していない)。それに伴い、`data/cards.json`・`data/cardpools.json`をユーザー自身の
テスト用プール/カードから空配列にリセットしたコミットは残っている(`git clone`直後
はカードプールもカードも0件から始まる。今後ユーザー自身が使う際は改めてカードプール
を作り直す必要がある点に注意)。

**配布形式は`pkg`(`@yao-pkg/pkg`、Vercel版`pkg`の保守フォーク)による単一exe化を
採用**(Electronは今回のような「軽く試してもらう」用途にはオーバースペックと判断し
見送った。モバイル対応は別途PWA経路があるため、デスクトップの配布形式の選択とは
無関係)。実装のポイント:

- `server.js`の`ROOT`を`ASSETS_ROOT`(静的アセット=`public/`用。読み取り専用でpkgの
  スナップショット内からでも問題なく配信できるので`__dirname`のまま)と
  `APP_ROOT`(書き込みが発生する`data/`・`images/`用。`process.pkg`が真ならexe自体の
  場所(`path.dirname(process.execPath)`)、それ以外(通常の`node server.js`実行時)は
  従来通り`__dirname`)の2つに分離した。pkgのスナップショットは基本読み取り専用の
  仮想ファイルシステムなので、実行時に書き込みが必要なデータ/画像はexeの隣の実フォルダ
  に置く必要がある。
- `package.json`に`"bin": "server.js"`、`pkg.assets: ["public/**/*"]`(静的アセットを
  スナップショットに同梱する指定)、`"build:exe": "pkg . --targets node22-win-x64
  --output dist/deck-viewer.exe"`を追加。`dist/`は`.gitignore`に追加済み(ビルド成果物
  なのでgit管理外)。
- `app.listen`のコールバックで`process.pkg`が真の場合のみ既定ブラウザを自動オープン
  する処理を追加(`start "" "url"` on Windows / `open` on macOS / `xdg-open` on Linux)。
  開発時(`npm start`/`npm run dev`)は`process.pkg`が存在しないため発火せず、再起動の
  たびに新しいタブが開くような邪魔は起きない。
- ビルド時の`--targets`指定でハマった点: `@yao-pkg/pkg` 6.21.0時点で、Windows向けの
  プリビルド済みNode本体バイナリはGitHubリリース(`yao-pkg/pkg-fetch`のv3.6タグ)に
  **node18/node20は存在せず、node22/node24/node26のみ**が置かれていた。node18/20を
  指定するとプリビルドが404になり、ソースからのフルビルドにフォールバックしようと
  して(Visual Studio Build Toolsが必要な`vcbuild.bat`が呼ばれ)失敗する。今後
  `--targets`を変える場合は、事前にGitHub リリースページ
  (`https://api.github.com/repos/yao-pkg/pkg-fetch/releases/tags/<タグ>`)で対象の
  `node-vX.Y.Z-win-x64`アセットが実在するか確認してから指定すること。
- ビルドしたexeは実機で動作確認済み(`dist/deck-viewer.exe`を単体で実行→
  ブラウザが自動的に開く→カードプール作成→カード追加(画像アップロード)まで一通り
  確認し、exeの隣に`data/`・`images/`フォルダが実際に生成され書き込まれることを確認)。
  検証用に作成したテストデータは確認後に削除済み。

## カードプールのエクスポート/インポート(2026-07-24)

「カードプールをまとめて持ち運びしたい」という要望への対応として、当初は
「`images/KMR`をGitHubにアップロードし、アプリからインポートボタンで直接DLできる
ようにしてほしい」という依頼があった。しかし実際に画像を確認したところ、
仮面ライダー(Union Arena TCG)の完全な公式カード画像(キャラクター写真・カード名・
テキスト・©表記まで全て含む、抽象化の形跡なし)だったため、著作権上の懸念を理由に
実装を見送った。ユーザーは続けて別の画像(アイドルマスター系、渋谷凛など実在キャラ名)
を提示したが、これも同種の懸念(既存作品のキャラクターイラストそのものに見える)を
理由に見送った。最終的にユーザーが「(`UA-maker`という自作ツールで)1から描いた
イラストだ、全部俺が作った」と明言したため、その申告を前提に進めることにした。

初期実装は「git commit/pushしたpool-exports/を別環境でgit pullしてインポート」という
git転送前提の設計だったが、直後にユーザーから2点の修正指示が入った: (1) 一般ユーザー
(exeを受け取る友人側)がエクスポートできる必要はなく、開発者側が事前に用意したものだけ
インポートできればよい、(2) 「git pull」を要求する時点でアウトで、**exeを受け取って
使う側は一切のコマンド操作(gitも含む)を経由せず、アプリの操作だけで完結**させたい。
これを受けて設計を変更した:

- `pool-exports/`はgitでの共有経路としてではなく、**pkgのexeビルドに静的アセットとして
  同梱するための場所**に位置づけを変更(`package.json`の`pkg.assets`に
  `"pool-exports/**/*"`を追加。`public/**/*`と同じ扱い)
- `server.js`の`EXPORTS_DIR`を`APP_ROOT`基準から**`ASSETS_ROOT`基準**に変更した
  (`public/`と同じ静的アセット扱い。exeパッケージ時はpkgのスナップショット
  [読み取り専用]から読む。開発時[`process.pkg`が偽]は`ASSETS_ROOT === APP_ROOT`
  なので今まで通りプロジェクト直下の`pool-exports/`を指す)。合わせて、起動時の
  `fs.mkdirSync(EXPORTS_DIR)`は`!process.pkg`のときだけ実行するようにした
  (読み取り専用スナップショット内にディレクトリを作ろうとするとエラーになるため)。
  `GET /api/pool-exports`も、フォルダ自体が存在しない場合に空配列を返すようガードした
  (exeにまだ何もバンドルしていない状態でも起動時にクラッシュしないように)
- 画像のコピー処理は`fs.copyFileSync`(スナップショット越しのコピーでpkgが正しく
  パッチしているか未検証だったため)をやめ、`fs.readFileSync`→`fs.writeFileSync`の
  組み合わせに統一(pkgのアセットバンドルで安全に動作することが確実な方の実装)
- **「エクスポート」ボタン自体を`pool-detail.html`/`pool-detail.js`から削除**した。
  エクスポートは開発者が`npm start`/`npm run dev`で通常起動した状態で
  `POST /api/pools/:id/export`をcurl等で直接叩く運用に変更(APIエンドポイント自体は
  残しており、UIからは呼べない)。その後`npm run build:exe`すると、その時点で
  `pool-exports/`にある内容がexeにバンドルされる
- ホーム画面の「カードプールをインポート」ボタン+一覧モーダルはそのまま維持。
  exeを受け取った側はこのボタンを押すだけで、バンドルされたプールを新規カードプール
  として取り込める
- **動作確認方法**: 開発サーバーでテストプール作成→`curl`でexport→
  (UIから「エクスポート」ボタンが消えていることを確認)→ライブのプールを削除
  (バンドル元データは別ディレクトリなので影響なし)→`npm run build:exe`→
  生成されたexeを**プロジェクトと無関係な別フォルダにexe単体だけコピー**して起動
  (`data/`/`images/`/`pool-exports/`など何もない状態を再現)→
  `curl http://localhost:<port>/api/pool-exports`でバンドル済みプールが見えることを
  確認→import実行→exeの隣に`data/`・`images/`が新規生成され、画像がバンドル元と
  バイト一致することを確認→ホーム画面の「カードプールをインポート」ボタン経由でも
  同じ一覧・インポートが機能することをUI操作で確認。検証用データ・exe・別フォルダは
  全て削除済み

同日中さらに、「フォルダに用意した画像と設定ファイルからインポート用データを作る」
という当初の要望(まだ未実装だった部分)に対応した。`manifest.json`を**必須では
なく任意**にし、以下の2パターンをサポートする形にした:
- `manifest.json`が無い場合: `pool-exports/<フォルダ名>/images/`内の画像を
  ファイル名の昇順で全て使い、カードプール名はフォルダ名、カード名は各画像の
  ファイル名(拡張子除く)がそのまま使われる
- `manifest.json`がある場合: `poolName`でプール名、`cards`配列(各要素`image`必須・
  `name`/`cost`は任意)で個別のカード名を上書き指定できる。`cards`内の各要素も
  `name`を省略した場合は画像ファイル名にフォールバックする(「アプリに登録済みの
  プールをエクスポートしたときに生成される`manifest.json`」と全く同じ形式なので、
  手書きでもエクスポート結果でもどちらでも同じインポート経路で読める)

サーバー側は`readManifest(folder)`(存在しなければ`{}`を返す。壊れたJSONは`null`)と
`resolveManifestCards(folder, manifest)`(`cards`配列があればそれを正規化、無ければ
`images/`をディレクトリスキャンして生成)の2つの共通関数に切り出し、
`GET /api/pool-exports`(一覧)と`POST /api/pool-exports/:folderId/import`(取り込み)
の両方から使うことで、判定ロジックの重複を避けた。実機で「画像のみ(manifest無し)」
「manifest.jsonで一部だけ名前を上書き」の両パターンを`curl`で往復確認済み
(前者はファイル名がそのままカード名になること、後者は指定した名前と
未指定分のファイル名フォールバックが両方正しく反映されることを確認)。検証用データは
削除済み。

同日中さらに、manifest.jsonの各カード項目に`cost`(必要エナジー)・`color`(色)・
`parallel`(パラレルかどうか)を追加できるようにした。これに伴い`cards.json`の
カードレコード自体にも`color`(文字列、既定は空文字)・`parallel`(真偽値、既定は
`false`)フィールドが加わった(`cost`は元々存在)。`POST /api/cards`(手動追加)・
`PATCH /api/cards/:id`にも同じ3フィールドの読み書きを追加し、`POST /api/pools/:id/export`
(エクスポート)も現在のカードレコードからこれらをmanifestへ書き戻すようにして
往復の整合性を保った。表示面では、これらの値が入っていても今まで見えない(＝機能が
無意味になる)のを避けるため、`pool-detail.js`の一覧表示`cardCaption()`に
「必要エナジー3・青・パラレル」のように追記するだけの最小限の表示を追加した
(グリッド表示のキャプションは変更していない。追加のUI[専用の入力欄など]は今回は
作っていない)。`curl`でmanifestに3フィールドを指定したケースをimport→表示確認→
export→manifestへの書き戻し確認、まで一通り実機検証済み。検証用データは削除済み。

同日中さらに`type`(character/event/fieldのいずれか)フィールドを追加した。
`CARD_TYPES = ["character", "event", "field"]`と`normalizeCardType()`で許可リスト
検証を行い、リストに無い値は空文字に丸める(`curl`で不正な値`"not-a-real-type"`を
渡すと空文字にフォールバックすることを確認済み)。データモデル・API
(`POST /api/cards`, `PATCH /api/cards/:id`, export, import)への追加はcolor/parallelと
同じパターンで一貫させた。表示は`pool-detail.js`に`CARD_TYPE_LABELS`
(character→キャラクター、event→イベント、field→フィールド)を追加し、
`cardCaption()`の先頭に差し込んだ(値が入っていれば「キャラクター・必要エナジー3・
青・パラレル」のように表示される)。

## UI刷新 第2弾(2026-07-24)

「タイトルを『デッキメーカー(仮)』に」「重複禁止で一意な名前に」「サムネイル指定」
「全画面グリッド表示+デフォルト化」「manifest.jsonでの絞り込み」「削除ダイアログの
自作ポップアップ化(以後全ポップアップ共通仕様)」という一連の要望に対応中。

- **タイトル変更**: 全HTMLの`<title>`・`index.html`の`<h1>`・
  `manifest.webmanifest`の`name`/`short_name`を「デッキメーカー(仮)」に統一。
- **専用確認ダイアログ(`public/js/dialog.js`)**: ブラウザ標準の`confirm()`を廃止し、
  `showConfirm(message, opts)`(Promise<boolean>を返す使い捨てモーダル)と
  `bindModalDismissal(overlay, {onCancel, onConfirm})`(既存の`.modal-overlay`に
  Enter/Esc/枠外クリックの共通挙動を後付けする)の2つを用意した。全ページの
  `<head>`直後(api.jsの次)で読み込む。複数モーダルが同時に開いている場合(例:
  カード追加モーダルの上にトリミングポップアップ)、後から`bindModalDismissal`
  登録された方を「一番上」とみなしてEnter/Escを渡す内部レジストリを実装
  (`bindModalDismissal`を呼ぶ順序が重要。実機で「トリミングポップアップだけ閉じて
  下のモーダルは残る」ことを確認済み)。今後追加するポップアップは全てこの2関数
  経由で実装すること、というのがユーザーからの明示的な指示。
- **カードプール/デッキ名の重複禁止**: `server.js`に`uniqueName(desired, existingNames)`
  を追加し、作成・リネーム・(プールインポート時の)命名で使用。衝突すると
  ` (1)`, ` (2)`...を自動付与(自分自身とは比較しないようリネーム時は除外)。
- **サムネイル指定(1枚まで、任意)**: `cardpools.json`の各プール・`decks/*.json`の
  各デッキに`thumbnailCardId`(nullable)を追加。`GET /api/pools`・`GET /api/decks`は
  実際の画像URLに解決した`thumbnailUrl`も返す(呼び出し側でカード情報を引き直さなくて
  良いように)。UIは`pool-detail.html`のグリッド表示の各タイル左上に★ボタン(トグル、
  クリックで即PATCH)、`builder.html`のデッキ側ペインの各カードにも同様の★ボタン
  (こちらはローカル状態のみで、「保存」を押すまでサーバーには送られない)。
  `manifest.json`にも`thumbnail`(画像ファイル名を指定)を追加し、インポート時に
  対応する新規カードIDを`thumbnailCardId`にセット、エクスポート時も書き戻す。

- **グリッド表示を全画面デフォルト化**: `pool-detail.html`のカード一覧のデフォルトを
  list→gridに変更。ホーム画面の「カードプール」「デッキ」両タブにも一覧/グリッドの
  切り替えトグル(☰/▦)を新設し、こちらもデフォルトgrid(localStorageキーは
  `deck-viewer-home-pool-view`/`deck-viewer-home-deck-view`で別々に記憶)。
  デッキの並び替えは**▲▼ボタンを廃止しドラッグに統一**した(一覧表示・グリッド表示の
  両方でドラッグ可能、`js/sortable.js`の`axis:"grid"`を流用)。デッキの一覧行にも
  カードプール同様のドラッグハンドルを追加。グリッドタイルのアクションは、カードプール
  側が★(お気に入り、画像オーバーレイ)+✎(リネーム)/🗑(削除)、デッキ側が
  ▶(表示)/✎(リネーム)/⧉(複製)/🗑(削除)という並び。グリッドタイルでの
  リネームは`startCaptionRename()`(キャプションをinputに差し替え、Enter/Esc/
  blurで確定・取消)を新設して対応(カード一覧で以前使っていたのと同種の仕組みを
  カードプール/デッキ名向けに復活させた形)。実機でグリッド/リストの切り替え、
  ドラッグ並び替え(カードプール・デッキとも)、リネーム、複製、カスタム削除
  ダイアログでの削除、を一通り確認済み。検証用データは削除済み。

- **カード追加/編集モーダルへのフィールド追加**: `pool-detail.html`のカード追加/編集
  モーダルに、タイプ(`<select>`: 未設定/character/event/field)・必要エナジー
  (数値)・色(自由入力テキスト)・パラレル(チェックボックス)を追加した。以前は
  これらの値はmanifest.jsonインポートかAPI直叩きでしか設定できなかったが、通常の
  UI操作だけで入力・編集できるようになった。`Api.addCard`にもcolor/parallel/type
  を渡せるよう拡張(`updateCard`は元々任意のpatchを送れる作りだったので変更不要)。
- **絞り込み機能**: `pool-detail.html`のカード一覧と`builder.html`の「手持ちカード」
  ペイン(デッキ側のペインは対象外)に、タイプ/色/必要エナジー/パラレルで絞り込める
  フィルターバーを追加した(「絞り込み」ボタンで開閉)。タイプは固定の3種類だが、
  色と必要エナジーは**実際にプール内に存在する値からピル(トグルボタン)を動的生成**
  する方式にした(色は自由入力のため事前に選択肢を用意できないため)。複数条件は
  AND(タイプ・色・コストはそれぞれのグループ内でOR、グループ間はAND)。
  `builder.html`側はフィルター対象を選択中のカードプールのカードに限定し、
  絞り込み中でもデッキへの追加(タップ/スワイプ)は問題なく機能する。両画面とも
  実機で単一条件・複数条件の組み合わせ・リセットを確認済み。検証用データは削除済み。

これで今回依頼された7項目(タイトル変更・専用ポップアップ・重複禁止の一意な名前・
サムネイル指定・全画面グリッド表示のデフォルト化・絞り込み・カード追加モーダルへの
フィールド追加)は全て実装・実機検証・コミット済み。

## 動作確認の記録について

2026-07-22〜08-05にかけての「どの画面のどの操作をブラウザで確認したか」という
詳細な記録がここにあったが、その後の作り直しで大半が現状と合わなくなったため
削除した(当時の作業内容は、下の日付付きセクションにそれぞれ残っている)。
以降の動作確認は各セクションの末尾に書く運用にする。

検証時の約束事(これは今も有効):

- 検証用に作ったカードプール/デッキ/カードは、確認が終わったら必ず削除する。
- **既存データを書き換える可能性のあるAPIは、絶対にユーザーの実カードで試さない**
  (過去に`POST /api/cards/:id/image`を実カードに対して叩き、画像を復元不可能な形で
  上書きしてしまった事故がある)。その場で作った使い捨てのプール/カードを使う。
- 開発サーバーを自分で起動する前に、既に動いていないか`curl`等で確認する。
  ポート3000をユーザー自身の`npm run dev`やexeが掴んでいることがある。

## 2026-07-28〜29のセッションでの大規模な追加(このファイルへの追記が長期間空いていたため、

> **⚠ このセクションの「カード情報自動取得」「トリガー自動判定」は2026-08-18に
> 機能ごと削除済み**(UIの入口が消えて到達不能な死にコードになっていたため)。
> 部品は`archive/card-info-autofill/`に退避してある(復活手順は同ディレクトリの
> README.md参照)。判定のしきい値やテンプレート座標のキャリブレーション経緯は
> 復活させる場合に必要なので、記録としてそのまま残している。
まとめて記録する。複数PC引き継ぎのため、以下は次のセッション/次のPCで必ず目を通すこと)

### カード情報自動取得(auto-fill-info)の継続的な改善

- **信頼度フラグ**: `costConfidence`(コスト判定の生スコア、閾値`COST_CONFIDENCE_THRESHOLD=0.5`)
  が低い場合、カードに`infoUncertain: true`を立て、一覧/グリッドに`⚠`バッジを表示する。
  手動で type/color/cost/trigger のいずれかを編集すると自動でfalseに戻る。
- **全画面共通の常駐パネル**(`public/js/card-info-jobs.js`、全ページで読み込み): 実行中/未確認の
  ジョブがあれば、どのページに移動しても右下に表示され続ける。折りたたみ(格納)機能があり、
  格納中の状態は`localStorage`(`deck-viewer-auto-fill-docked`)に保存されページ遷移をまたいで
  維持される。ジョブが完了した瞬間は、格納されていても自動的に展開される。他のカードプールの
  ジョブが実行中/未確認の間は、別のプールで新規実行しようとするとブロックされ通知が出る。
- **進捗バー**: 実行中は0.7秒間隔でポーリング(アイドル時は4秒)。パラレルカードは実際には
  分類されずベースカードの結果をコピーするだけなので、`progress.total`はパラレルを含まない
  実分類対象数のみで計算する(以前はパラレル込みの総数だったため、後半で進捗が一気に
  100%になる不具合があった)。残り時間(ETA)は直近20件の処理時刻からローカルなペースで
  算出する(ジョブ開始からの累計平均だと、V8のJITウォームアップの影響で最初の数枚が
  遅く、それが全体を通じて平均を引きずり下ろし続け、実時間の約2倍速でカウントダウンする
  不具合があったため)。
- **「既にある情報を上書きする」チェックボックス**はデフォルトでON(以前は`openAutoFillFor()`が
  毎回falseにリセットしてしまうバグがあった)。
- **不確実カードのレビューフロー**: `⚠`ボタン(自動取得ボタンの左)を押すと、`infoUncertain`な
  カードを1枚ずつ「カードを編集」モーダルで確認できる。「戻る」/「保存して次へ」(最後の
  カードは「保存して完了する」)ボタン付き。ベースカードには「変更をパラレルにも適用する」
  チェックボックス(デフォルトON)があり、ONの場合そのカードのパラレルはレビュー対象から
  除外され(残り枚数の分母も減る)、OFFの場合は個別にレビュー対象として現れる。レビューを
  最後まで終えると、右下の完了通知も自動的に確認済み扱いになる(通知が消え、自動取得ボタンが
  緑でなくなる)。完了通知の「修正する」ボタン(旧「再度実行する」)を押すと、そのカードプールの
  詳細画面に遷移し(`?review=1`)、自動的にこのレビューフローが開く。

### トリガー自動判定(新機能、2026-07-28〜29)

`tools/trigger-templates/`(active/drow/final/get/raid/special/color、一部`_old`違いあり)を
使い、カードの下部にある「トリガー」バッジを自動判定する。`classify-cards.js`の
`classifyTrigger()`が本体。色・タイプ・コストの判定(Stage A〜C)の後、Stage Dとして実行される。

- **判定の前に「そもそもトリガーがあるか」を先に判定するゲートがある**: トリガーが実在する
  カードは下部の特定の狭い領域(600x838キャンバス上でy:745〜770, x:225〜375)に赤い
  「トリガー」の文字が入っている。この領域の赤ピクセル数(`r>190 && g<60 && b<60`、
  かなり厳しい閾値)が300未満なら、バッジ種類の判定を一切せず即座に`trigger: ""`
  (トリガーなし)を返す。**この閾値は実データで検証済み**: 仮面ライダー(KMR)224枚+
  キングダム(KGD)123枚、計347枚の実カードの正解データに対して100%一致。
  - 経緯: 最初はもっと緩い赤判定(`r>140 && r-g>40 && r-b>40`)で実装したが、[UA] 魔都精兵の
    スレイブの2枚(UA49BT_MSS-1-039, -078)がトリガーなしなのにアクティブと誤判定される
    バグがあった(白背景がテンプレートの白文字部分と偶然マッチしていた)。この赤ゲートを
    追加して解決。ところが同じ緩い閾値をキングダムに適用すると**全カードが「トリガーあり」
    と誤判定**された。原因はキングダムのカード枠に、トリガー文字と同じ場所に無関係な
    オレンジ寄りの赤(平均RGB≈179,80,40)があったため。本物のトリガー文字は
    平均RGB≈230,23,28とG/B成分が極端に低く、`g<60 && b<60`まで閾値を厳しくすることで
    両者を正しく分離できた。
  - **他の作品(まだ検証していない配色のカード)を追加する際は要注意**: この座標・閾値は
    今回検証した作品群(仮面ライダー/キングダム/魔都精兵のスレイブ/チェンソーマン/犬夜叉/
    陰の実力者になりたくて！/無職転生)では機能するが、カード枠のデザインが大きく異なる
    新しい作品を追加してトリガー判定がおかしい場合、まずこの赤ピクセル領域・閾値を
    実データで再検証すること(トリガーあり/なしの実カードから該当領域のRGB値をサンプリング
    して比較する、というやり方は`classify-cards.js`の該当コメントに詳しい)。
- **バッジ種類の判定**(ゲートを通過した後): 各トリガー種類のテンプレート画像との
  マスク済み平均差分を、位置ズレを許容するスライディングウィンドウ探索(`maskedDiffScoreSearch`,
  `TRIGGER_SEARCH_PAD=6`, `TRIGGER_SEARCH_STRIDE=2`)で比較する。これはコストの数字判定で
  使っている手法と同じ(実カードとテンプレートの数px単位の位置ズレが、この手の細かい
  テキスト比較では致命的にスコアを狂わせるため)。
  - **さらに、僅差(スコア差5未満)の候補が複数ある場合のタイブレーク**がある
    (`TRIGGER_TIEBREAK_MARGIN=5`)。実例: 「レイド」と「ドロー」は見た目上ほぼ同じバナー
    (半透明で背景アートが透けるデザイン)のため、カードによってはバナー全体比較だと
    両者のスコア差が1〜2点しかなく、ほぼコイントスで負けることがあった(実際に
    チェンソーマンの3枚が誤判定されていた)。僅差の場合のみ、バッジ左端にある小さい
    不透明な「レイド」「ドロー」ラベル部分だけ(`TRIGGER_LABEL_BOX_WIDTH=150`px、
    こちらは全体探索よりさらに細かい`PAD=8, STRIDE=1`)で再比較し、そちらの結果を採用する。
    このタイブレークを実装する際、既存の粗い探索設定(PAD=6, STRIDE=2)を小さいラベル領域に
    使い回したところ別の誤判定を引き起こしたため、専用の細かい設定に分離した。
  - `TRIGGER_MAX_DIFF_SCORE=50`(ゲートを通過したがどの種類にも十分一致しない場合の
    「トリガーなし」判定用)は**未検証の初期値**(コストの閾値と違い、正解データでの
    キャリブレーションはまだ行っていない)。
  - 最終的に5つのカードプール(仮面ライダり224枚、チェンソーマン80枚、キングダム123枚、
    犬夜叉80枚、陰の実力者になりたくて！80枚)の非パラレルカード計587枚全てで
    自動判定と正解データが一致することを確認済み。
- データモデル: `cards.json`に`trigger`フィールド(空文字列=トリガーなし、または
  active/drow/final/get/raid/special/color)。`normalizeTrigger()`, `TRIGGER_TYPES`。
  manifest.jsonのインポート/エクスポート、`POST/PATCH /api/cards`、パラレルへの
  継承・一括適用(`applyToParallels`)全てに対応済み。
- UI: `pool-detail.html`のカード追加/編集モーダルに「トリガー」セレクト、絞り込みサイドバーに
  トリガーのチェックボックス群(「トリガーなし」を含む8項目)を追加済み。
  ~~**`builder.html`(デッキ編集画面)側にはまだ追加していない**~~
  → **後のセッションで追加済み**(`builder.html`の`#filter-trigger-group`、
  `builder.js`の`filterState.triggers`)。

### カードプールのエクスポート/インポート(.dvpool形式のzip、2026-07-28〜29)

- バックエンド: `GET /api/pools/:id/export-zip`(manifest.json+images/をその場でzip化して
  ダウンロード。拡張子`.dvpool`は単なるリネームzipで、OSに汎用アーカイブとして誤解されて
  勝手に展開されるのを避ける目的)、`POST /api/pools/import-zip`(アップロードされたzipを
  一時フォルダに展開し、既存の`pool-exports/`インポートと同じ`importPoolFromFolder()`を
  共有して使う)。
- **重要なバグ修正**: zipインポートのエンドポイントが、カード画像1枚分(15MB上限)用の
  Multerインスタンスを使い回していた。実際に296枚のカードプール(仮面ライダー)を
  エクスポートすると63MBあり、当然15MB制限に引っかかっていた。zipインポート専用に
  500MB上限の`uploadZip`という別のMulterインスタンスを用意して解決。あわせて、
  Multerのエラーがそのまま漏れるとExpressのデフォルトHTMLエラーページが返り、
  クライアント側の`res.json()`が「Unexpected token '<'」で失敗する(原因がわかりにくい
  典型パターン)ため、Multerのエラーを捕まえてJSONで返すエラーハンドリング
  ミドルウェアを`server.js`の末尾(`app.listen`の直前)に追加した。
- UI: ホーム画面の「カードプールをインポート」の右に「カードプールをエクスポート」
  ボタンを追加(プール一覧から選んでダウンロード)。「カードプールをインポート」
  モーダルの一覧の先頭に「＋ ローカルからカードプールをインポート」の行を追加
  (ファイル選択→アップロード)。
- `pool-exports/`内の7つのフォルダ(CSM/IYS/KGD/KJN/KMR/MSS/MST、いずれもUAシリーズの
  カードプール)を全て`.dvpool`形式に変換し、git管理下に追加した(2026-07-29、
  このセッションの最後の作業)。生フォルダ自体(`pool-exports/<name>/manifest.json`+
  `images/`)は**引き続きgit管理外のまま**(pkgでexe化する際の同梱元として使われる
  従来の仕組みは変更していない、単に`.dvpool`という持ち運び用の追加コピーを作っただけ)。
  KMR.dvpoolは60MBあり、GitHubの推奨上限50MBは超えているが、ハード上限100MBは超えて
  いないため通常のpushで問題なく成功している(Git LFSへの移行は今のところ未実施)。
- ~~**デッキ側のzipエクスポート/インポートはまだ未着手**~~
  → **2026-08-01に`.dvdeck`として実装済み**(下記「デッキのzipエクスポート/
  インポート(2026-08-01、完了)」参照)。

### デッキ編集画面(builder.html)の機能追加

- **undo/redo**: 画面下部の操作バーに「元に戻す」「やり直す」(Ctrl+Z/Ctrl+Y対応)。対象は
  デッキの中身(カード追加・削除・並び替え・コストソート・サムネイル設定)のみで、
  デッキ名や参照プールの選択は対象外。履歴は最大30件。
  - **重大なバグ(修正済み)**: undo/redo用の履歴配列をグローバルスコープで`history`という
    変数名にしてしまい、ブラウザ標準の`window.history`オブジェクトを覆い隠していた。これにより
    デッキ保存後の`history.replaceState(...)`(URLをid付きに書き換える処理)が
    「history.replaceState is not a function」で毎回失敗していた。変数名を`deckHistory`に
    変更して解決。**他の画面で同様のグローバル変数を追加する際、`history`・`location`・
    `navigator`など組み込みオブジェクト名との衝突に注意すること**(今回のような分かりにくい
    バグの原因になる)。
- **サムネイル設定/統計情報表示の統合**: 従来「サムネイルを設定」ボタン単体だったものを、
  丸い☰メニューボタン(`#deck-menu-btn`)+プルダウン(`#deck-menu-dropdown`)に統合し、
  「サムネイルを設定」「統計情報を表示」の2項目を格納した。
- **統計情報を表示**ポップアップ: 必要エナジー分布(コスト0〜7+「8+」のバーチャート、
  枚数ラベルはバーの上の専用の行にまとめて表示しバーと重ならないようにしている)と、
  トリガー内訳(表示順は「トリガーなし・ゲット・ドロー・アクティブ・レイド・カラー・
  スペシャル・ファイナル」で固定、右端基準の横棒グラフ+枚数はバーの右外側の固定列
  (縦一直線に揃う))。開いた瞬間に0から素早く伸びて減速するアニメーション付き
  (`ease-out`のCSS transition。**`requestAnimationFrame`ではなく`setTimeout(fn, 20)`で
  トリガーしている**点に注意 — 下記「はまりどころ」参照)。
- **サムネイル未設定時の点滅インジケーター**: メニューボタン(三本線部分のみ)と、
  プルダウン内「サムネイルを設定」項目(文字部分のみ)が、青グラデーションでゆっくり
  点滅する(枠自体は静止した青グラデーション、点滅するのは中身だけ、という仕様)。
  **この見た目の実装は3回作り直した**(下記「はまりどころ」に詳細)。最終的に、
  三本線はSVGを完全にやめて`<span>`3本(明示的width/height+background)にし、
  ボタンの`padding:0`+`gap`による均等配置で決着した。
- **サムネイル選択モード**: 対象カードにマウスを乗せると青い枠が付く(既存の浮き上がり
  ホバー演出はそのまま)。ボタン文言は「サムネイルを設定」→(選択中)「選択をキャンセル」
  (赤文字・赤枠)に変更。この際、メニュー化する前の名残で`#deck-thumbnail-mode-btn.active`
  というIDセレクタのCSSルール(オレンジ背景)が残っており、クラスベースの新しい赤スタイルを
  IDセレクタの詳細度で上書きしてしまっていたため削除した。
- **デッキ/カードプール両ペインが常に左右並びになるよう修正**(以前は画面幅700px未満で
  縦並びに崩れていた)。

### サムネイルマーカーのデザイン(pool-detail.html一覧/グリッド + card-render.js)

★の丸バッジから、カード全体を囲む黄色い枠+「THUMBNAIL」帯に変更済み(このファイルの
もっと前の記録にある通り)。この後のセッションでさらに調整し、**最終形**は: 枠は
`.card-frame.is-thumbnail::after`でカードにぴったり密着(`inset:0`、はみ出さない)、
帯は`.card-item.is-thumbnail::before`でカードの上端から数px飛び出す形の塗りつぶし
バナー(文字は白・小さめ)。一度「外側に少しずらした枠」を試したが、
オフセット+aspect-ratioの組み合わせで下端・右端がずれて「カードから浮いて見える」
バグになり撤回した経緯がある。調整したい場合はこの2つのCSSルール
(`public/css/style.css`内の`.card-frame.is-thumbnail`/`.card-item.is-thumbnail`)を見ること。

### その他の細かい追加

- カード追加/編集モーダルの画像プレビューに拡大表示(⤢)ボタンを追加(グリッド表示と
  同じライトボックスが開く)。
- 「プロモファイナルを除外」フィルター(色が「全て」のカードを除外、デフォルトON、
  プール内に該当カードがある場合のみ表示)を`pool-detail.html`に追加済み。

## デッキのzipエクスポート/インポート(2026-08-01、完了)

カードプール側の`.dvpool`と同じ形式・実装パターンで、デッキ単位のエクスポート/インポートを
追加した。拡張子は`.dvdeck`(中身は`.dvpool`と同じくただのzip)。

- バックエンド: `GET /api/decks/:id/export-zip`(デッキが参照するカードのうち画像が実在する
  ものだけをmanifest化してzip化。デッキ自体は特定のプールのカードIDを参照するだけで
  ポータブルではないため、エクスポートはカード本体のデータ+画像を`deck.json`(トップレベル
  キーは`deckName`/`thumbnail`/`exportedAt`/`cards`、各cardは`name`/`cost`/`color`/
  `parallel`/`type`/`trigger`/`count`/`image`)にまるごと同梱する)、`POST /api/decks/import-zip`
  (アップロードされたzipを展開し、**インポート専用の新規カードプールを1つ自動作成**して
  そこにカードを再登録(新規ID採番)した上で、新しいデッキを作りそのプールだけを参照させる。
  `uploadZip`(500MB上限、カードプールのzipインポートと共用)を使うため、カード画像1枚用の
  15MB上限Multerとは衝突しない)。既存の`importPoolFromFolder`と同様、`uniqueName`で
  デッキ名・プール名の重複を回避。
- UI: デッキ編集画面(`builder.html`)の☰メニューに「デッキをエクスポート」
  (未保存(`deckId`なし)の場合はエラーメッセージを出すだけで何もしない)「デッキをインポート」
  (ファイル選択→`showConfirm`で「現在の変更は破棄されますがよろしいですか？」を確認してから
  実行→成功したら新しいデッキのURLに遷移)を追加。デッキ一覧画面(`index.html`)の
  デッキタブにも「デッキをインポート」ボタン(確認ダイアログなし、その場の一覧に追加されるだけ
  で今開いている作業を破棄するわけではないため)を追加。`js/api.js`に
  `exportDeckZipUrl(deckId)`/`importDeckZip(file, name)`を追加(カードプール側の
  `exportPoolZipUrl`/`importPoolZip`と同じ形)。
- 検証: 開発サーバーで使い捨てのプール/デッキ(3枚・複数枚のカードを含む)を作成→
  export-zipでダウンロード→**元データを削除した状態**でimport-zipに投入→新規プール+
  新規デッキが生成され、カード名・枚数・画像拡張子・サムネイル参照が元と一致することを
  `curl`で確認、を2回(直接API・builder.htmlのメニュー経由の両方)実施。検証用データは
  全て削除済み。ブラウザの`<input type="file">`はこの自動化ツールではネイティブの
  ファイル選択ダイアログを操作できないため、実際のクリック→ファイル選択までは
  ユーザー側での実機確認が望ましい(コード自体は動作確認済みのカードプールインポート
  ボタンと全く同じパターンで実装しているため、リスクは低いと判断)。

## お気に入り機能をカードプール・デッキ両方に拡張(2026-08-01、完了)

以前からあった「カードプールのお気に入り」フラグ(データモデルのみ存在、UIなし)を
実際に使えるようにし、デッキにも同じ機能を追加した。

- **バックエンド**: デッキ側に`favorite`フィールドを追加(`cardpools.json`同様、既定は
  未設定=false扱い)。`PATCH /api/decks/:id`を新設(`favorite`のみ対応。カードプールの
  `PATCH /api/pools/:id`と違い、デッキは今まで部分更新の口がなく`POST /api/decks`の
  全体upsertしかなかったため)。`GET /api/decks`の一覧レスポンスにも`favorite`を追加。
  カードプール側は元々`PATCH /api/pools/:id`が`favorite`を受け付ける実装済みだったので
  バックエンド変更は不要だった。
- **UI**: 各アイコンボタン列(一覧表示の行・グリッド表示のタイル、カードプール/デッキ
  両方)に★(お気に入り)トグルボタンを追加(`.icon-btn.favorite`、オン時は
  `#f5a623`で塗り、テキストも☆→★に切り替え)。一覧描画時、お気に入りを常に先頭にまとめる
  ソートを追加(お気に入り同士・非お気に入り同士の中の順序はドラッグ並び替えの順序を
  維持する安定ソート)。
- **「お気に入りのみ表示」ボタン**: ホーム画面のカードプールタブ・デッキタブそれぞれの
  ツールバーに追加(`.favorites-only-btn.active`でオレンジ背景、状態は`localStorage`に
  保存)。さらに、デッキ編集画面(`builder.html`)のカードプール選択欄(参照するカード
  プールを複数選択するトグルボタン一覧)にも同じボタンを追加し、お気に入りのカード
  プールだけに絞り込める(絞り込みは選択肢の表示/非表示だけに影響し、既に選択済みの
  プールの選択状態自体は維持される。他の絞り込み機能と同じ挙動)。
- 実機で、カードプール/デッキそれぞれの★トグル、一覧内でのお気に入りが先頭に来る並び替え、
  「お気に入りのみ表示」フィルター(3画面: ホームのカードプールタブ・デッキタブ・
  builder.htmlのカードプール選択欄)を全て確認済み。検証中に実ユーザーデータ
  (カードプール「[UA] 魔都精兵のスレイブ」)のfavoriteフラグを一時的にtrueにしたが、
  確認後trueのまま残さずfalseに戻し済み。

## 開発サーバーの多重起動に関する注意(このセッションで踏んだ事故)

このセッション中、ユーザー自身が`npm run dev`(nodemon経由でserver.jsを起動)を既に
実行中の状態で、Claude側が動作確認のために独自に`node server.js`をバックグラウンドで
起動し、さらに`pkill -f "node server.js"`で再起動を試みた。nodemonは親プロセス
(`nodemon.js`)と実際にserver.jsを実行する子プロセスが別PIDになっており、
`pkill -f "node server.js"`は子プロセスだけを巻き込んで終了させてしまい、nodemonの
親プロセスは残ったままなのに実際にリクエストを処理するサーバーがいなくなる、という
事故が発生した(Claude自身が起動した方の`node server.js`インスタンスがport 3000を
確保して動いていたため一見動作しているように見えたが、そちらを止めた瞬間に
サーバーが完全にダウンした)。**今後、既に起動中の可能性がある開発サーバーに対して
`pkill -f "node server.js"`のような広いパターンでのプロセス終了は行わないこと**。
動作確認用に自分でサーバーを起動する場合は、まず`curl`等で既存サーバーの有無を確認し、
既に動いていればそれを使う(再起動が必要な変更は、ユーザーの`npm run dev`が
ファイル保存を検知して自動再起動してくれるはずなので、基本的に自分で再起動する
必要自体がない)。今回は最終的に`node server.js`を手動で起動し直してサービスを復旧
させたが、ユーザーには純粋な`nodemon`のファイル監視・自動再起動が効かない状態に
なっている可能性がある旨を報告し、`npm run dev`の再起動を推奨する必要がある。

## モバイルからのLANアクセス・PWAインストールの検証(2026-08-01、調査完了)

実機(スマホ等の別デバイス)は用意できなかったため、同一PC上で「LAN側からのアクセスを
模した状況」を作って検証した。結論: **アプリ自体はLAN経由のhttpアクセスで問題なく
動作する。ただしPWAインストール(ホーム画面に追加、Service Workerによるオフライン
キャッシュ)はLAN上の平文httpでは原理的に不可能**(ブラウザ側の仕様上の制約であり、
このアプリのバグではない)。

- **サーバーのバインド・ファイアウォールは問題なし**: `server.js`は元々
  `app.listen(PORT, "0.0.0.0", ...)`で全インターフェースにバインドしている
  (`server.js:1186`)。開発機のLAN IP(`172.16.30.106`)に対して`curl`で疎通確認したところ
  `200`が返り、Windows側のファイアウォールにも`node.exe`向けのInbound Allowルールが
  既に(Publicプロファイル含めて)存在していることを`Get-NetFirewallRule`で確認した。
  現在アクティブなネットワークプロファイルは`Public`カテゴリだったが、そのルールが
  既にカバーしていた。
- **Service Worker登録がLAN上のhttpで機能しないことを実機同等の条件で確認**: Claude
  Browserペインで`http://172.16.30.106:3000`(localhostではなくLAN IP)を直接開き、
  `window.isSecureContext`と`"serviceWorker" in navigator`を確認したところ、それぞれ
  `false`だった。ブラウザは`localhost`/`127.0.0.1`だけを例外的に「secure context」と
  みなす仕様のため、LAN IPでの平文httpアクセスでは`navigator.serviceWorker`自体が
  露出せず、`register-sw.js`の`if ("serviceWorker" in navigator)`分岐が単に素通りされて
  エラーも出さずService Worker登録がスキップされる(既存コードは既にこの状況で
  クラッシュしない安全側の書き方になっていた)。ページ自体(タイトル・基本的な描画)は
  この状態でも正常にロードされることも確認済み。
- **今後この制約を解消したい場合の選択肢**(今回は実装していない、方針決め待ち):
  (1) `mkcert`等でLAN内自己署名証明書を発行しhttps化する(各デバイスにルート証明書を
  インストールする手間が発生し、「ポチポチ操作だけで使いたい」という方針とは相性が
  悪い)、(2) Tailscale等のVPNでhttpsやMagicDNS経由にする(セットアップの手間が増える)、
  (3) LANでのPWAインストールは諦め、「同一Wi-Fi内であればブラウザのタブとしては
  普通に使える」という位置づけのまま据え置く。現状は(3)のまま。

## トリガー判定`TRIGGER_MAX_DIFF_SCORE=50`のキャリブレーション状況(2026-08-01、調査済み・現状維持)

> **⚠ この判定ロジックは2026-08-18に削除済み**(`archive/card-info-autofill/`に退避)。
> 復活させる場合のためにキャリブレーションの記録だけ残している。

現在ライブの`data/cards.json`に載っている全カードプール(仮面ライダー・魔都精兵の
スレイブ・チェンソーマン・犬夜叉・陰の実力者になりたくて！・キングダム)の非パラレル
カード計669枚に対し、一時的なスクリプトで`classifyImage()`(`tools/classify-cards.js`、
内部で`classifyTrigger()`を呼ぶ)を実際に再実行し、各カードの現在保存されている
`trigger`値(=これまでの自動判定+人間によるレビューを経て確定している値)と突き合わせて
`TRIGGER_MAX_DIFF_SCORE=50`のしきい値が実際に効いているかどうかを検証した(検証用
スクリプトは確認後に削除済み、データの書き換えは一切行っていない)。

- 669枚全てで最終的なトリガー判定(トリガーなし/種類とも)が現在の保存値と100%一致。
- 赤ゲート(`TRIGGER_LABEL_MIN_RED`)を通過し、実際に何らかの候補スコアが得られた
  509枚について`triggerScore`(採用された最良スコア=小さいほど良い一致)の分布を
  確認したところ、最大値は`37.16`で、`50`という閾値に対して約13ポイントの余裕が
  あった。しきい値50付近(40〜60)に張り付いているケースは1件もなし。
- 結論: **現状保有している669枚のデータの範囲では、しきい値`50`がボーダーライン
  として機能する場面は一度も発生しておらず、値を変える必要性は見つからなかった**
  (=このしきい値は「ゲートは通過したが、どのテンプレートにも十分近くない」という
  保険的な分岐で、現状のデータでは一度も発動していない)。従って閾値`50`は現状維持
  とする。ただし「実際にトリガー判定がおかしい」という報告が今後あった場合、まず
  その対象カードを`classifyImage()`で個別に再実行し、`triggerScore`が50付近に
  張り付いていないか確認するところから始めること(50付近に張り付いていれば
  閾値調整の実データが初めて手に入ったことになる。全く別の値であれば別原因
  ―テンプレート不足・未検証の配色など―を疑うこと)。

## お気に入りUIの追加調整(2026-08-01、上記の続き)

お気に入り機能を追加した直後のセッションで、ユーザーからの実機フィードバックを受けて
何度か細かい調整を行った。

- **カードプール選択欄(builder.html)のレイアウト崩れ修正**: 「お気に入りのみ表示」を
  オン/オフした際、該当プールが0件だと「お気に入りのカードプールがありません」の
  空メッセージが表示され、これが通常のカードプール一覧行より縦に長くなってレイアウトが
  ガクッと動く問題があった。`.pool-checkbox-list`を`min-height`ではなく固定`height`にし、
  空メッセージ側も汎用の`.empty-state`が持つ🂠アイコンの`::before`を`display:none`で
  消して1行に収めることで解決。ついでに「カードプールが1行に入りきらない場合は
  2行目を作らず横スクロールする」という要望もあり、`.pool-toggle`に`flex-shrink:0`+
  `white-space:nowrap`を追加(元々flex-wrap:wrapで折り返していたのをnowrap+
  overflow-x:autoに変更した際、ボタン自体が縮んで文字が内部で2行に折り返さり、結果的に
  高さが崩れるという不具合も併発していたため合わせて修正)。横スクロールバーが出た
  瞬間にコンテンツの上端が欠ける問題も発覚し(固定高さがスクロールバー分の余白を
  考慮していなかったため)、コンテナの高さに余裕を持たせて解決。
- **一覧のお気に入り優先ソートは撤回**: 最初「お気に入りを常に先頭に」というソートを
  入れたが、「並び順は変わらずでお気に入りだけ切り替えたい」という指摘を受けて削除
  (`home.js`の`renderPools`/`renderDecks`からソート処理を撤去)。
  以降、★のオン/オフは表示順に一切影響しない。
- **★ボタンをサムネイル画像の右上に移動**: 当初グリッドタイル下のアイコン列(✎🗑と
  並び)に置いていたが、「サムネイルの右上に配置して」という指摘で、`.card-frame`
  (サムネイル画像を包む要素)内に`position:absolute; top:4px; right:4px;`で重ねる形に
  変更した(`.card-frame-favorite-btn`。`createFavoriteBtn`/`createDeckFavoriteBtn`に
  `{overlay:true}`オプションを追加し、一覧行では従来通りアクション列に、グリッドタイルの
  場合だけサムネイル上のオーバーレイにする、という出し分けにした)。一覧表示は
  サムネイル自体を新規追加したので(下記参照)そちらは変更していない。
- **★の視認性バグ(CSS詳細度)**: サムネイル上のオーバーレイに移した際、非選択状態の
  ☆が`--muted`(暗めのグレー)で表示され、半透明の暗い背景に重なってほぼ見えなくなる
  不具合があった。原因は`.icon-btn.favorite`(クラス2つ、詳細度0-2-0)の`color`指定が、
  後から書いた`.card-frame-favorite-btn`(クラス1つ、詳細度0-1-0)より詳細度で勝って
  しまい、色指定が上書きされていたこと。実際にDOM上で付与される3クラス全部
  (`.icon-btn.favorite.card-frame-favorite-btn`)を指定するセレクタに直して解決し、
  合わせてオン/オフどちらの状態でも常に白い枠線が付くようにして、どんなサムネイル
  画像の上でも視認できるようにした。**今後similar overlay系のボタンを追加する際、
  共通クラス(`.icon-btn`など)とオーバーレイ専用クラスを両方持つ要素は、CSS詳細度の
  ぶつかり合いに注意すること**(実際に付与される全クラスをセレクタに含めるのが安全)。
- **ホーム画面ツールバーの並び順変更**: カードプールタブは「お気に入りのみ表示→
  カードプールをインポート→カードプールをエクスポート→表示切替(☰▦)」、デッキタブは
  「お気に入りのみ表示→デッキをインポート→表示切替(☰▦)」の順に変更(元々表示切替が
  先頭にあったのを末尾に移動。`.list-toolbar`が`justify-content:flex-end`なので、
  HTML内の並び順がそのまま画面上の左→右の順になる)。
- **一覧表示(横並びの行)にもサムネイルを追加**: 従来、一覧表示は左のドラッグハンドルに
  続けて名前・枚数のテキストのみだったが、ドラッグハンドルと名前テキストの間に小さい
  サムネイル画像を追加した(`.card-row-thumb`、`pool-detail.js`のカード一覧が既に
  使っていたクラスをそのまま流用。`createRowThumb()`をhome.jsに新設し、
  `createPoolRow`/`createDeckRow`両方に組み込んだ)。

これらの変更を通じて確立した運用パターン: **CSS/JSを本番デプロイなしに実ブラウザで
確認する際、`<link>`のhrefにcache-bustクエリを付けて差し替えることでHTTPキャッシュを
確実に回避できる**(Service Workerのキャッシュクリアだけでは不十分な場合があった)。
また、**動作確認用に自分でサーバーを起動する前に、必ず`curl`等で既存サーバーの有無を
既に確認する**運用を徹底し、以降このセッションでは開発サーバーを誤って落とす事故は
再発していない(前回のセッションで一度事故を起こした反省を踏まえた対応)。

## リポジトリを再度public化し、カードプールインポートをGitHub直DL方式に刷新(2026-08-02)

「公開状況(2026-07-24)」で一度private運用に戻したが、このセッションでユーザーから
「git上に.dvpoolを多数用意し、カードプールをそこからDLする形でインポートすることに
したい、全てUI上で解決するように、既存のpool-exportsからインポートする形は廃止で」
という要望があった。private repoのままだと匿名でのraw file取得ができず(exeを受け取る
友人側にGitHub認証を持たせる必要が出てしまい、トークンをexeに埋め込む方式は誰でも
抜き出せてしまうセキュリティ上の懸念がある)、この要望とは相性が悪かったため、事前に
「exe配布相手にもこの機能を使わせたいか、privateのままでよいか」を確認したところ
「publicリポジトリにするつもりだった、もうしていい」との回答を得て、**リポジトリを
再びpublicに変更した**(`gh repo edit --visibility public`)。

これに伴い、旧来の「開発者があらかじめ`pool-exports/<名前>/`フォルダ(または
`POST /api/pools/:id/export`で生成した生フォルダ)を用意し、`npm run build:exe`で
pkgのスナップショットにバンドルする」という配布方式は完全に廃止した:

- **バックエンド**: `GET /api/pool-exports`・`POST /api/pool-exports/:folderId/import`・
  `POST /api/pools/:id/export`(生フォルダへのdev専用エクスポート)を削除。代わりに
  `GET /api/github-pools`(GitHub Contents API `https://api.github.com/repos/<owner>/<repo>/
  contents/pool-exports?ref=<branch>` を叩き、`.dvpool`ファイルだけを抽出して
  `{name, poolName, size}`の一覧を返す)と`POST /api/github-pools/import`
  (`{fileName}`を受け取り、ファイル名を`/^[A-Za-z0-9._-]+\.dvpool$/`で検証した上で
  `https://raw.githubusercontent.com/<owner>/<repo>/<branch>/pool-exports/<fileName>`
  から生バイト列を取得し、一時フォルダに展開して`.dvpool`ローカルアップロード
  (`POST /api/pools/import-zip`)と同じ`importPoolFromFolder()`を共有して使う)を新設した。
  リポジトリ名・ブランチは`GITHUB_REPO`/`GITHUB_BRANCH`定数(`satoa-may5/deck-viewer`/
  `master`)にハードコード。Node 22時点で`fetch`はグローバルに使えるため追加の依存関係は
  不要だった。`EXPORTS_DIR`・起動時の`pool-exports/`ディレクトリ作成処理も不要になり削除。
- **`package.json`**: `pkg.assets`から`"pool-exports/**/*"`を削除(もうexeに同梱しない。
  実行時にGitHubから都度取得するため)。
- **フロントエンド**: `js/api.js`の`getPoolExports`/`importPoolExport`を
  `getGithubPools`/`importGithubPool`に置き換え、`home.js`の「カードプールをインポート」
  モーダル(`openImportModal`)がGitHubの一覧を表示するように変更(カード枚数の代わりに
  ファイルサイズ[KB/MB]を表示。GitHubの一覧APIはファイルサイズしか返さず、枚数を
  出すには全ファイルをダウンロードする必要がありコストが見合わないため)。「＋
  ローカルからカードプールをインポート」(ローカル`.dvpool`ファイルのアップロード)は
  この変更と無関係の別機能なのでそのまま維持。
- **実機検証**: 開発サーバーで`GET /api/github-pools`が実際に7つの`.dvpool`
  (CSM/IYS/KGD/KJN/KMR/MSS/MST)を正しいサイズ付きで返すこと、`POST /api/github-pools/import`
  で実際にMST.dvpool(110枚)をGitHubから取得→展開→新規カードプールとして取り込めること
  (カード名・画像拡張子が正しいことも確認)、旧`/api/pool-exports`が404になったことを
  `curl`で確認。ブラウザ上でも「カードプールをインポート」モーダルが7件のファイルサイズ
  付き一覧を表示することを確認済み。検証用に作成したテストプールは削除済み。
- **今後の運用**: 新しいカードプールを配布ラインナップに追加したい場合、通常通り
  ホーム画面の「カードプールをエクスポート」ボタンで`.dvpool`をダウンロード→
  `pool-exports/<名前>.dvpool`として`git add`・commit・push するだけでよい
  (`npm run build:exe`でのバンドル作業や`curl`でのdevエンドポイント直叩きは不要になった)。
  生の`pool-exports/<名前>/manifest.json`+`images/`フォルダ自体は`.dvpool`を作るための
  ソースとしてローカルに残しているが、git管理外・exeバンドル対象外のまま(今後
  `.dvpool`を作り直す際の元データとして使う以外の役割はない)。

## カード詳細フィールド拡張: レアリティ/AP/BP/特徴/発生エナジー/効果 + カード名の分離(2026-08-02)

ユーザーが`pool-exports/IMC`(アイドルマスター シンデレラガールズ)・`pool-exports/MST`
(無職転生、既存の`MST.dvpool`とは別の新フォーマット版)という2つの生フォルダを新規に
用意し、manifest.jsonにこれまでシステムが扱っていなかった項目(`rarity`, `ap`, `bp`,
`attribute`, `generatedEnergy`, `effect`)を大量に含めてきたことをきっかけに、これらを
データモデル・UI(追加/編集モーダル・絞り込み・検索)に正式対応させた。

- **`name`/`cardName`の役割分離(重要な破壊的変更)**: 新manifestでは`name`が実際の
  カード名(例:「千川 ちひろ」)を意味するようになった。一方、アプリの内部フィールド
  `name`(既存)はCARD-001式の自動採番・パラレル判定(`parseCardNameParts`)・キャプション
  表示のフォールバックに使われ続けており、これまでmanifestインポート時は
  `item.name || 画像ファイル名`で埋めていた。この2つの役割がmanifest上で衝突するように
  なったため、**`resolveManifestCards()`を変更し、内部`name`は常に画像ファイル名
  (拡張子除く)から生成し、manifestの`name`は新設の`cardName`(実際のカード名)に
  マッピングするようにした**。これは新旧問わず全てのmanifestインポート経路
  (GitHub `.dvpool`ダウンロード・ローカル`.dvpool`アップロード・デッキ`.dvdeck`
  インポート)に共通で適用される。既存のOLDなmanifestで`name`にカスタムの表示名を
  入れていたケースも、今後はその値が`cardName`側に入るだけで データは失われない
  (むしろカード名として正しく扱われるようになる)。エクスポート側
  (`GET /api/pools/:id/export-zip`, `GET /api/decks/:id/export-zip`)も対応して
  `manifest.cards[].name`に`card.cardName`を書き出すよう変更し、往復(エクスポート→
  インポート)でcardNameが保持されることを実機確認済み。
- **UI表示側**: `displayName()`(pool-detail.js)・`card-render.js`の共通キャプション
  生成が`card.cardName || card.name || "(名称未設定)"`の優先順位になった。既存の
  カードプール(cardNameが無い)は今まで通り`name`(識別コードや自動採番名)が表示され、
  回帰なし。
- **追加/編集モーダルのフィールド追加**: 既存の「カード名(任意)」入力欄は
  「識別コード(任意)」にラベル変更(元々の役割=CARD-XXX等の識別用。データキーは
  `name`のまま変更なし)。新たに「カード名(任意)」(=`cardName`)を最上部に追加。
  さらにレアリティ・消費AP・BP・発生エナジー・特徴(カンマ区切りテキスト、配列として
  保存)・効果(複数行テキストエリア)の入力欄を追加した。
- **絞り込み(pool-detail.html / builder.html 両方に同一実装)**:
  - レアリティ・消費AP・特徴・発生エナジーは、固定の選択肢ではなくプール内の実データ
    から動的にピルを生成する方式(`distinctValues()`)。特にレアリティは`C`, `U`, `R`,
    `SR`, `UR`に加え`R★`, `SR★★★`のような派生表記が多数あり、固定enumにできないため。
  - BPは「スライダー方式が好ましい」という要望通り、必要エナジーと同じ二重レンジ
    スライダー(0〜6000、ステップ100)にした。ただしBPの値は`"2000+"`/`"4000-"`
    (以上/以下を表す接尾辞)や無効値`"-"`を含む自由文字列のため、`parseBpValue()`
    (pool-detail.js/builder.js に同一実装)で先頭の数値部分だけを抜き出して比較に使う
    (接尾辞は表示上そのまま残す)。
  - 必要エナジーの二重レンジスライダーは元々1画面に1つしか無い前提の実装
    (`updateCostSliderUI`, `filterCostSliderWrap`等の専用変数・関数)だったが、BP用に
    ほぼ同じ80行超のドラッグ処理(タイブレーク処理込み、CLAUDE.mdの過去の教訓が
    複数反映された繊細なロジック)をもう一つ必要としたため、**`setupRangeSlider()`
    という汎用コントローラに切り出した**(pool-detail.js・builder.js それぞれに実装。
    2ファイル間の共有はしていない、この2ファイルはそもそもモジュール化されておらず
    重複を許容する既存方針のため)。既存のCost用スライダーもこの共通関数経由に置き換え、
    動作は変えていない(実機で必要エナジー・BP両方のドラッグ操作を確認済み)。
  - 「カード名/効果で検索」という自由テキスト検索欄を追加(`filterState.searchQuery`、
    `card.cardName`と`card.effect`の両方を対象に部分一致、大小文字区別なし)。実機で
    「渋谷 凛」を検索すると、カード名が一致するもの以外に、効果テキストに「渋谷 凛」を
    含む別カード(島村 卯月のカード)もヒットすることを確認(意図通り、cardNameと
    effectのどちらか一方にマッチすればよい仕様)。
- **サーバー側の正規化**: `normalizeAttribute()`を新設(JSON文字列(multipart経由)・
  実配列(JSON body経由)のどちらでも受け付け、非文字列/空文字要素を除去)。`rarity`/
  `bp`/`generatedEnergy`/`effect`はいずれも自由文字列としてtrimのみ、`ap`は
  `cost`と同じ数値/null。`applyToParallels`(不確実カードのレビュー画面で使う、
  ベース→パラレルへのtype/color/cost/trigger一括反映機能)は**今回の新フィールドには
  拡張していない**(この機能は画像からの自動判定フィールド専用で、rarity/ap/bp等は
  manifestインポートでしか埋まらない値のため、対象外のままが自然と判断)。
- **実機検証**: `pool-exports/IMC`(188枚)・`pool-exports/MST`(110枚、新フォーマット版)
  をそれぞれ一時的にzip化して`.dvpool`ローカルインポート経由で取り込み、
  (1)`cardName`に実際のキャラ名(例:「千川 ちひろ」)、`name`に画像ファイル名由来の
  識別コードが入ること、(2)グリッドキャプションが`cardName`優先で表示されること、
  (3)レアリティ11種・特徴7種・消費AP・BP・発生エナジーの絞り込みピル/スライダーが
  実データから正しく生成され、実際に絞り込めること(レアリティ=SRで15枚、curlで
  取得した生データの件数と一致することを確認)、(4)編集モーダルに全フィールドが
  正しく事前入力されること、(5)MSTプールをエクスポート→削除→再インポートで
  `cardName`等が完全に往復することを確認。検証用に作成したテストプールは全て
  削除済み。実機確認は全てpool-detail.js・builder.jsの両方で実施(絞り込みUIを
  2画面で揃えるという既存方針を踏襲)。

なお、この検証の過程で気付いた点: 既存のコミット済み`pool-exports/MST.dvpool`
(旧フォーマット、rarity等のフィールドを含まない)が、このセッション中にユーザー側の
操作でファイルシステムから削除されていた(`git status`で`D pool-exports/MST.dvpool`
として検出、このセッションのどの操作でもClaude側からは触れていない)。新しい
`pool-exports/MST/`生フォルダ(新フォーマット)に置き換えるための削除と見られ、
ユーザーの意図的な操作として扱いコミットに含めた。新しい`MST.dvpool`を配布
ラインナップに再度含めたい場合は、アプリでインポート→(必要なら)「カードプールを
エクスポート」で作り直し、`pool-exports/MST.dvpool`としてcommit・pushし直す必要がある
(このセッションでは実施していない、ユーザー側の判断待ち)。

## カードプールをインポート一覧の高速化: pool-exports/index.json 事前生成方式(2026-08-04)

以前の`GET /api/github-pools`は、サーバー再起動直後(=キャッシュが空)だと
GitHubのContents APIで一覧を取った後、新規/更新分の`.dvpool`ごとに本体
(数十MBのzip)を丸ごとダウンロードしてmanifest.jsonだけを読む、という重い処理を
していた。これがサーバー再起動直後の初回アクセスが遅い原因だった。

**`tools/build-pool-export-index.js`**を新設し、`pool-exports/*.dvpool`を
スキャンして`poolName`/`release`/`size`をまとめた`pool-exports/index.json`を
生成するようにした。`server.js`の`listGithubPools()`は、GitHubのContents API +
per-file manifest読み込みをやめ、この`index.json`をraw.githubusercontentから
1回fetchするだけになった(60秒の簡易TTLキャッシュ付き)。

**今後の運用で重要**: `pool-exports/*.dvpool`を追加・更新するたびに、
以下を実行してから一緒にコミット・プッシュすること。実行し忘れると
`index.json`が実際の`.dvpool`と食い違ったまま(古いpoolName/release/sizeが
表示され続ける、新しく追加した`.dvpool`が一覧に出てこない)になる。

```
node tools/build-pool-export-index.js
```

なお、サーバー起動直後の初回アクセスそのものが遅い(GitHubへの初回fetch自体に
時間がかかる)問題は、`index.json`化によって「1回のfetchで軽量なJSONを取るだけ」
になったことで大幅に改善するはずだが、それでも初回の1回だけは何らかの待ち時間が
残る(起動時のpre-warmと5分ごとの定期リフレッシュで、実際にユーザーが画面を開く
頃には大抵完了している想定)。

## pkgビルドのexeが起動直後にクラッシュする問題の修正(2026-08-04)

ユーザーから「exeを実行してもcmdが一瞬開いてすぐ閉じる」と報告があった
(ダブルクリックだと標準出力が一瞬で消えて原因が見えない典型パターン)。
`dist/deck-viewer.exe`を`cmd`/ターミナルから直接実行して原因を特定:

```
Error: Cannot find module 'C:\snapshot\deck-viewer\node_modules\@jimp\plugin-print\dist\commonjs\load-font.js'
```

`server.js`が起動時に`require("./tools/classify-cards")`(カード情報自動取得
機能で使用)を読み込み、そのtools/classify-cards.jsが`jimp`(画像処理ライブラリ、
v1.6.1)を require している。jimpのプラグイン(`@jimp/plugin-print`)が内部で
フォントファイルを**動的な(リテラルでない)requireパス**で読み込んでおり、
pkgの静的解析ではこの動的requireを追跡できず、スナップショットにその
モジュールが同梱されないままになっていた(通常のnode実行では問題にならず、
pkgでexe化したときだけ顕在化するタイプの不具合)。

`package.json`の`pkg.assets`に`"node_modules/@jimp/**/*"`・
`"node_modules/jimp/**/*"`を追加して解決した(pkgは`assets`に指定したファイルを
生スナップショット内に同梱し、`fs`をパッチして読み取れるようにするため、
動的requireでも実行時にファイル自体は見つかるようになる)。修正後、exeを
プロジェクトと無関係な別フォルダにコピーして`cmd`から実行し、正常に起動
(`deck-viewer server running: ...`)・`curl`で200応答・`data/`/`images/`が
exeの隣に生成されることを確認済み。

**今後の教訓**: pkgでビルドしたexeが「一瞬で終了する/何も起きない」場合、
まずダブルクリックではなく`cmd`/PowerShellから直接実行してエラー出力を
確認すること(ダブルクリックだとウィンドウが即座に閉じてエラーが読めない)。
`Cannot find module` 系のエラーは大抵、依存ライブラリが内部で動的requireや
プラグイン読み込みをしている場合に起きる(pkgの静的解析が追跡できないため)。
今後jimpや同様のプラグイン機構を持つライブラリを新規に追加する場合、
exeビルド後に一度必ず実機(ダブルクリックではなくターミナルから)起動確認を
行うこと。

## カード一括追加フロー・未編集バッジ・デッキ編集の左右クリック化(2026-08-04)

- **「カードの情報を自動取得する」ボタンを削除**(`pool-detail.html`のツールバーから)。
  `card-info-jobs.js`のパネル本体・`requestAutoFillPanel()`・サーバー側の分類処理は
  一切手を付けておらず、今はボタンという入口が無いだけ(将来また別の場所から
  呼び出す可能性を見込んでコードは残してある)。
- **カードを追加の入口をモーダル選択式に変更**: ＋ボタン→「カードを1枚追加する」
  (従来通り)/「カードを複数枚追加する」(新規)の2択ポップアップ。複数枚追加は、
  選んだ画像をそのまま(トリミングなし)一覧に並べ、タイルごとの✂アイコンで
  個別にトリミングポップアップ(既存の`#crop-popup`を`cropContext`で共有)を開ける。
  「完了」で画像1枚につき1枚のカードを一括登録し、名前だけCARD-XXXを連番で自動採番、
  それ以外の情報(タイプ・色・必要エナジー等)は空のまま(登録後にカード一覧から
  個別に編集してもらう想定)。
- **`unedited`フラグ**: 複数枚追加で作られたカードに立つ(`data/cards.json`の
  カードレコードに新フィールド)。`PATCH /api/cards/:id`(カード編集モーダルの保存)
  を一度でも通れば`false`にリセットされる。`unedited`が立っているカードは
  一覧・グリッド両方の右上に「未編集」バッジを表示(既存の自動取得「⚠」バッジより
  優先、両方trueになることは実運用上ほぼ無い想定)。
- **デッキ編集画面のクリック挙動を統一**: 左クリック(タップ)=デッキに1枚追加、
  右クリック(`contextmenu`、モバイル長押しでも発火)=デッキから1枚削除、を
  デッキ側・カードプール側どちらのペインでも同じに揃えた(以前はデッキ側タップ=
  削除、カードプール側タップ=追加、という非対称な割り当てだった)。既存の
  `attachTap()`(ドラッグ並び替えとの誤判定を防ぐ距離しきい値ロジック)を
  `attachCardClicks({onAdd, onRemove})`に拡張し、`pointerdown/pointerup`は
  `e.button===0`(左ボタン)の時だけ追跡、`contextmenu`は`preventDefault()`して
  削除に割り当てる形にした。拡大表示(⤢)ボタンなど、カード本体の上に重なる
  子要素は右クリックのバブリングも止めないと誤ってカードが削除されてしまう点に注意
  (実際に一度踏んだ: `pointerdown`/`pointerup`/`click`だけ`stopPropagation`して
  `contextmenu`を止め忘れていたバグを追加で修正した)。
- **デッキ編集画面に案内バーを追加**: 「デッキ」「カードプール」ペインの下、
  「元に戻す」「やり直す」の上に「左クリックで追加、右クリックで削除できます」を
  横幅いっぱいで表示。✕で閉じると`localStorage`に恒久的に記録され、以後どの
  デッキを開いても二度と表示しない(デッキ単位ではなくアプリ全体で1回きりの案内)。
- **実機検証時の事故と教訓**: 動作確認中、`curl`で叩いたポート3000が実は開発サーバー
  (`npm run dev`)ではなく、直前のexeリリース検証で起動しっぱなしだった
  `dist/deck-viewer-1.0.0/deck-viewer-1.0.0.exe`だったため、コード変更後もずっと
  古い(exeスナップショット内の)HTML/JSが返り続け、変更が反映されていないように
  見える現象に少しハマった。`Get-Process`で該当PIDを特定して停止し、改めて
  `npm run dev`を起動して解決。**exeの動作確認用に一時的に起動したプロセスは、
  検証が終わったら確実に止めること**(ポート3000を掴んだまま放置すると、次の
  セッションでの`npm run dev`ベースの動作確認が古いビルドに対して行われてしまい、
  気づきにくい形で検証全体が無意味になる)。テストデータ(カードプール
  `__test_bulk_add__`、テスト用カード2枚)は確認後に削除済み。

## 細かい修正: showConfirm誤用・exe二重起動・入場アニメーション(2026-08-05)

- **`showConfirm()`は`checkboxLabel`を渡さない場合、素の真偽値(true/false)を
  resolveする**(`{confirmed, checked}`ではない)。`builder.js`の「保存せずに
  戻る」警告と`.dvdeck`インポート確認の2箇所が`result.confirmed`を見ていたため、
  `checkboxLabel`未指定→`result`はただの`true`/`false`→`result.confirmed`は常に
  `undefined`→「戻る」を押しても何も起きない、という分かりにくいバグになっていた
  (`js/dialog.js`の`resolve(checkboxLabel ? {confirmed, checked} : result)`参照)。
  `checkboxLabel`を渡さない`showConfirm()`呼び出しは戻り値を直接真偽値として
  扱うこと。
- **pkgでビルドしたexeを二重起動すると、後から起動した方はEADDRINUSEで
  クラッシュしてブラウザも開かず一瞬でウィンドウが消える**(前回のウィンドウを
  閉じ忘れている状態で再度ダブルクリックした場合など)。`server.js`の
  `app.listen()`が返す`http.Server`に`error`イベントリスナーを追加し、
  `EADDRINUSE`かつ`process.pkg`が真の場合は新しくサーバーを起動する代わりに
  既存サーバーのURLをブラウザで開いて`process.exit(0)`するようにした。
  - **この修正自体に2段階のバグがあった**(両方とも実機/実プロセスでの検証で
    発見・修正済み)。(1) `openBrowser()`→`process.exit(0)`を同期的に連続で
    呼んでいたが、`child_process.exec()`は子プロセスの起動を待たずに即座に
    returnする非同期処理(実測で子プロセスが実際に立ち上がるまで約60ms
    かかる)なので、直後に`process.exit()`するとブラウザが実際に開く前に
    イベントループごと強制終了してしまう可能性があった → `exec()`の
    コールバックを待ってから`process.exit()`するように変更。(2) その修正で
    `openBrowser(url, callback)`とし`exec(command, callback)`のように呼んだ
    ところ、通常起動時(`callback`省略→`undefined`)に**pkgでビルドした
    exeだけ**`TypeError: The "callback" argument must be of type function.
    Received an instance of Object`でクラッシュした(素のNode.jsでは
    `exec(command, undefined)`は問題なく通る。pkgの`child_process`
    シム/bootstrap.js側の引数チェックがより厳格らしい)。`callback`が実際に
    渡された時だけ`exec(command, callback)`、それ以外は`exec(command)`と
    呼び分けることで解決。**pkgでビルドしたexeの动作は都度`node server.js`
    ではなく実際にビルドしたexeで確認すること**(通常のnode実行では発生
    しない類のバグが今回も再現した)。実機で、通常の単独起動・同じポートへの
    二重起動の両方を確認し、二重起動時もクラッシュせず1つ目のサーバーが
    無傷であることを確認済み。
- **カードのタップ操作に「うっすら入ってくる」入場アニメーションを追加する際、
  対象要素は`renderPanes()`による再描画の"後"に探し直す必要がある**:
  `addToDeck()`/`removeFromDeck()`は呼び出し内で`renderPanes()`を実行して
  グリッドの中身を丸ごと作り直すため、クリックされた古いDOM要素の参照は
  再描画後には既に外れている。両関数に`afterRender`コールバック引数を追加し、
  `findCardTile(container, cardId)`(`data-id`属性で再検索)で新しい要素を
  取得してからアニメーションを仕込むようにした。

## セキュリティ・堅牢性の修正 + 自動取得機能の削除(2026-08-18, v1.0.2)

コード全体の見直しで見つかった問題への対応。前半3件は実際に攻撃/操作を再現してから
直し、修正後に同じ手順で塞がったことを確認している。

### パストラバーサル(重大)

`/api/decks/:id`がURLパラメータをそのままファイル名にしていた。**Expressは
ルートパラメータをパーセントデコードする**ため、`/api/decks/..%2F..%2Fpackage`は
ハンドラに`"../../package"`として届き、`path.join`で`DECKS_DIR`の外に出られた。

- `GET` → 任意の`.json`が読める(`package.json`が返ることを実測)
- `DELETE` → 任意の`.json`が消せる(カナリアファイルが実際に消えることを実測)

しかも`app.listen(PORT, "0.0.0.0")`なので**同じLAN上の誰でも**実行できた。
デッキのパス組み立てを全て`deckFile(id)`に集約し、`ID_PATTERN`を通らないIDを弾く
ようにした(このパターンは区切り文字も`:`も許さないので、通った時点で
`DECKS_DIR`直下のファイルしか指し得ない)。**今後URLパラメータをファイル名に
使う箇所を足すときは必ず同じ検証を通すこと。**

### ID衝突(潜在的なデータ破損)

カードIDは`card-${Date.now()}`だけで採番していた。カードIDは画像ファイル名も
兼ねるため、衝突すると**別のカードの画像を黙って上書きする**。「複数枚追加」は
連続POSTするので現実的なリスクだった(30件を`Date.now()`で採番するシミュレーションでは
ユニークID 1/30 まで潰れる)。`uniqueId(prefix, takenIds)`を新設し、
プロセス内の単調増加カウンタ + 既存IDとの突き合わせの二重で保証する(片方だけだと、
カウンタが実時間より先に進んだ状態で再起動したときに穴が残る)。pool/card/deck の
全採番箇所をこれに置き換え、**30並列POSTで30/30ユニーク**になることを確認済み。

### 悪意ある`.dvpool`からの任意ファイル読み出し

`importPoolFromFolder`が`path.join(imagesFolder, item.image)`をそのまま使っており、
manifestに`"image": "../../../../package.json"`と書けば展開先の外を読んで
`images/`(静的配信されている)へコピーできた。`path.basename()`で潰し、
拡張子もpng/jpgのallowlistで弾くようにした。実際に traversal 2種 + 偽拡張子を
仕込んだ`.dvpool`を作ってインポートし、正規の1枚だけが取り込まれ`images/`に
何も漏れないことを確認済み。

### マリガンシミュレーターが古いデッキを引き続ける

`mulliganFullPool`は「新しく7枚引く」の瞬間に固定され、リセットボタン以外で
クリアされなかった。統計画面を閉じ→デッキを編集→再度開くと、盤面もドロー対象も
編集前のまま(実測で盤面7/7・山札25/25が現在のデッキに無いカード)。
`deckCompositionSignature()`で開始時点の構成を控え、再オープン時に変わっていた場合
だけ破棄するようにした。カードIDでソートした署名なので、**並び替えただけでは
リセットされない**(編集せず開き直した場合も手札は保持)。

### 「検索条件をリセット」が初期状態に戻らない

`excludeAllColor`(プロモファイナルを除外)だけ初期値が`true`なのに、リセットは
`false`にしていた。結果、リセット後の方が初期状態より表示が増えるという直感に
反する挙動になっていた。builder.js・pool-detail.js 両方で初期値の`true`に戻すよう修正。

### カード情報の自動取得を削除

「カードの情報を自動取得する」ボタンを外した時点で`requestAutoFillPanel()`の
呼び出し元が消え、判定エンジン・ジョブAPI・常駐パネル・⚠レビューフローが丸ごと
到達不能な死にコードになっていた。それでも`server.js`が起動時に無条件で`jimp`を
読み込んでいて、起動+85ms・exe+23MB・pkgビルドが壊れやすい(v1.0.0のexeが起動即
クラッシュした原因)という負担だけが残っていたため、ユーザー判断で**丸ごと削除**した。

- 削除: `POST /api/pools/:id/auto-fill-info`, `GET /api/card-info-jobs`,
  `runAutoFillInfoJob`, `parseCardNameParts`, `applyToParallels`(PATCH),
  `infoUncertain`の読み書き, ⚠バッジ/レビューフロー, 関連CSS
- 依存からも`jimp`を外し、`pkg.assets`の`node_modules/@jimp|jimp`指定も削除
- 部品は`archive/card-info-autofill/`へ退避(`classify-cards.js`・テンプレート画像・
  `card-info-jobs.js`。復活手順は同ディレクトリのREADME.md参照)
- 効果: 起動 約190ms→約105ms、exe 80.9MB→57.9MB、配布zip 31.9MB→22.2MB

**注意**: `cards.json`に残っている`infoUncertain`フィールドは読まれなくなっただけで
削除はしていない(既存データを書き換えないため)。テンプレート座標・閾値の
キャリブレーション経緯は上記の該当セクションにそのまま残してある。

## オリカメーカー(UA-maker)統合(2026-08-19)

> **⚠ このセクションの「ローカル限定」に関する記述は後に全て撤回済み。**
> 素材はユーザー本人が作成したものであることを確認し、`public/oricard/`は
> git管理対象・配布用exeにも同梱する方針に変更した。ビルド時に退避する必要も無い。
> 経緯と現行の方針は後述の「オリカメーカーのローカル限定方針を撤回、公開版にも
> 含めることに変更(2026-08-19)」を参照すること。以下は当時の記録として残す。

`C:\Users\user\Desktop\UA-maker`(ユーザーが別プロジェクトとして作っていた、
Union Arena風のカードを合成生成するツール)を、カードプール個別ページの
「カードを追加」フローに統合した。「オリカを追加する」ボタン(選択モーダルの
一番上)を押すとiframeでツール本体が開き、「このカードを追加する」を押すと
canvas描画結果をPNG化してそのカードプールに新規カードとして登録する。

### 著作権上の理由でローカル限定にしている

`UA-maker/materials/`(コストジェム・トリガーバナー等、204ファイル)の中身を
実際に確認したところ、独自イラストではなく**公式カードから切り出したと見られる
素材**だった(トリガーバナーの書体・文言、コストジェムのデザインが公式そのもの)。
このリポジトリは既にpublicで、exeもGitHub Releasesで友人に配布しているため、
「ローカル限定で統合する」というユーザーの選択に従い:

- `public/oricard/`(index.html + materials.js、統合の実体)と
  `oricard-source/`(HANDOFF.md + 生のmaterials/フォルダ、将来の素材追加・
  materials.js再生成用)を`.gitignore`で除外。**publicリポジトリには一切
  上がらない**
- `public/sw.js`の`SHELL_FILES`にも意図的に含めていない(存在しない環境で
  Service Workerのインストール自体が失敗するため)
- `pool-detail.js`側は`public/oricard/index.html`の存在を`fetch(HEAD)`で
  毎回確認し、無ければ「オリカメーカーが見つかりません」の案内だけ出して
  静かに機能を諦める(他の人がこのリポジトリをcloneしても壊れない)

**このマシンでのみ動く機能である点に注意**: `npm run build:exe`で配布用の
exeを作る前には、`public/oricard/`を一時的に退避してからビルドすること
(gitignoreされているだけで、ディスク上に存在すればpkgは`public/**/*`の
glob経由でそのまま拾ってexeに同梱してしまう)。忘れると、友人に配ったexeに
公式カード由来の素材が入ってしまう。

### 技術的なポイント

- UA-maker本体(`index.html`)は元々スタンドアロンページとして作られており、
  `state`・`canvas`はトップレベルの`const`/`let`のため、外側のiframeから
  `iframe.contentWindow.state`のようにアクセスできない(トップレベルの
  `let`/`const`はグローバルオブジェクトのプロパティにならない仕様のため)。
  スクリプト末尾に1行だけ`window.exportOricard=function(){return
  {state,canvas,COLORS,CNAME};};`を追加し、これだけを頼りに外から状態を
  読み出している。**UA-maker本体のロジックには一切手を入れていない**
  (元のスタンドアロン利用に影響しない)。
- UA-makerの`state`(色キーB/G/P/R/Y、種類character/event/field、トリガー
  active/drow/final/get/raid/special/color+none等)は、このアプリの
  カードスキーマとほぼ1対1で対応していたため、画像だけでなく
  タイプ・色・必要エナジー・消費AP・BP・トリガー・特徴・効果テキストも
  一括で転記している(「複数枚追加」のような画像のみ登録ではなく、
  `unedited`は立てない)。色キー→日本語ラベルの変換はハードコードせず、
  UA-maker自身の`COLORS`定義から引いている。
- 生成画像は`canvas.toBlob()`(PNG)をそのまま`Api.addCard()`の
  `imageBlob`として送信、既存の「カードを1枚追加する」と同じAPIを共有。

実機確認: iframe読み込み(204素材ロード完了)→フォーム入力→「追加する」→
カードプールに正しいメタデータ(色・タイプ・必要エナジー・特徴・効果・
インライン画像トークン込み)で登録され、画像も実際の合成結果になっている
ことを確認。存在しない環境を想定した(`fetch`をモックした)フォールバック
表示も確認。検証用に作成したテストカードは削除済み。

## UA-makerイラスト機能拡張(2026-08-19、ヘッダー削除・タブ化・イラスト3種追加)

「以降ビルドするときってどうすればいい？」という質問と合わせて、`public/oricard/
index.html`(UA-maker本体)への追加要望が入り、対応した。(当時はこの機能を
ローカル限定と位置づけていたため以下の記述はその前提になっているが、後に撤回済み。)
変更は当時gitignore対象だった
`public/oricard/index.html`・`public/oricard/materials.js`のみに閉じている。

- **ヘッダー削除**: `<header><h1>オリカメーカー</h1></header>`を削除。
- **①〜⑧をタブ化**: 縦積みの`.g`セクションを`.tabbar`(ボタン列)+`.tabpanel`
  (`.on`クラスで表示切替)に変更。番号(①〜⑧)は削除しラベルのみに
  (色/発生エナジー・種類/コスト・イラスト・カード名/特徴・効果テキスト・
  トリガー・レイド・BP)。「保存/読み込み」セクションのみ番号が元々無かったので
  タブ化せず最下部にそのまま残した。レイド/BPタブは`state.type==='character'`
  でない場合`tabbtn`ごと`display:none`にし(旧実装は`.g`自体を隠していたのを
  タブボタンを隠す方式に変更)、非表示になった瞬間そのタブがアクティブだったら
  自動的に「色/発生エナジー」タブへ切り替える(`switchTab('color')`)処理を追加。
- **必要エナジーのヒントテキストのレイアウト修正**: `#reqHint`
  (「この色・種類で使える値のみ：0, 1, ...」)が`.two`の左半分カラムに閉じ込められ
  変な位置で折り返していた問題を、`.two`行の外(下)に出して全幅で表示するように
  変更(`.row.two`の外に`<div class="hint" id="reqHint">`を移動しただけ、JS側の
  文言生成ロジックは無変更)。
- **イラスト機能を4種に拡張**(旧: 単一の「イラスト」→カードイラストに改名 + 新規3種)
  ※この項で触れている`ILL`/`TEXTAREA_ILL`という座標定数は、後のセッションで
  「マスク画像のアルファから実行時に算出する」方式に置き換えられ廃止済み
  (後述「オリカメーカーのUI/UX細部修正」参照):
  - **カードイラスト**: 既存の`ILL={x:38,y:113,w:524,h:373}`のまま(座標変更なし)。
  - **カード名イラスト**(新規): `Main-components/Cardname_base.png`(600x838、
    名前バナー部分だけが不透明なマスク画像)の不透明部分だけにクリップして配置。
    実装は「オフスクリーンcanvasにイラストをcover配置→
    `globalCompositeOperation='destination-in'`でCardname_base.pngを重ねてマスク→
    本canvasに合成」という2パスの手法。続けて`Main-components/Cardname_frame.png`
    (同じく600x838のマスク画像、名前バナーの縁取り部分)を**色・明度を自由に
    調整できる状態で**上に重ねる。色調整は`globalCompositeOperation='color'`
    ブレンド(色相・彩度だけ入れ替え、明暗の陰影は維持)、明度調整は`drawImage`前に
    `ctx.filter='brightness(N%)'`を適用、その後`destination-in`で元のframe画像の
    アルファ形状に再クリップ(colorブレンドの`fillRect`が全面不透明を上書きして
    しまうため、最後に必ず形状を復元する必要がある)。UIには色ピッカー
    (`<input type="color">`)と明度スライダー(30〜200%)を追加。
  - **背景イラスト**(新規): 全コンポーネントより下(`render()`の最初、
    `Base_image.png`より前)に、キャンバス全面(`0,0,CW,CH`)へcover配置。
  - **テキストエリアイラスト**(新規): `TEXTAREA_ILL={x:37,y:494,w:526,h:260}`
    (ユーザー指定の座標。既存の効果テキスト領域`EFF`とほぼ重なる)にクリップして
    配置。効果テキスト(`drawEffect()`)より前に描画し、テキストが上に乗って
    読めるようにしている。
  - 4種とも同じドロップ&ズーム/パンUIを共有するため、`makeIllustration(key)`
    という汎用ファクトリ関数に切り出した(`drop-${key}`/`file-${key}`/
    `imgtools-${key}`/`zoom-${key}`/`panx-${key}`/`pany-${key}`/`clear-${key}`の
    命名規則でDOM要素を探す)。`state.illust.{card,name,bg,textarea}`にそれぞれ
    `{data,zoom,panx,pany}`を保持。JSON保存/読込にも対応させ、**旧形式
    (単一の`state.imgData/zoom/panx/pany`)のJSONを読み込んだ場合は自動的に
    `state.illust.card`へ移行する**マイグレーション処理を`applyState()`に追加
    (後方互換、過去に保存したJSONファイルが読めなくなることを防ぐ)。
- **素材の追加とmaterials.js再生成**: ユーザーが`C:\Users\user\Desktop\UA-maker\
  materials\Main-components\`に`Cardname_base.png`/`Cardname_frame.png`/
  `Text_area.png`を新規追加していたため(このセッション開始時点で204→207ファイルに
  増加)、`oricard-source/HANDOFF.md`記載のPowerShell再生成コマンドを実行して
  `materials.js`を作り直し、`public/oricard/materials.js`と`oricard-source/
  materials/`の両方に反映した。**今後も`C:\Users\user\Desktop\UA-maker\materials\`
  に素材を足した場合は、この再生成コマンドを実行してから`public/oricard/
  materials.js`にコピーし直す必要がある**(自動化されていない、手動手順)。
- **検証方法の注意**: この変更の検証中、ポート3000は開発用`npm run dev`ではなく
  **ユーザー自身が使っている可能性のある`dist/deck-viewer-1.1.0/
  deck-viewer-1.1.0.exe`(Phase G当時にビルドしたローカル専用版)が掴んでいた**
  ことが判明した。pkgのexeスナップショットはビルド時点で凍結されているため
  ライブ編集中の`public/oricard/index.html`の変更を反映できず、かつユーザーが
  実際に使用中の可能性があるプロセスを誤って落とすリスクがあったため、**ポート3000
  には一切触れず、別ポート(3930)で`node server.js`を単独起動して検証専用に使い、
  検証後は明示的にそのPIDだけを停止した**。Claude Browserペインの
  `computer{screenshot}`はこのセッションでも「ペインが表示されていないと
  フレームを合成しない」制約で使えなかったため、`javascript_tool`でのDOM/
  canvas直接検査(タブ切り替えの`display`、`materials.js`のキー数、
  `getImageData()`でのピクセルサンプリングなど)で機能面(クラッシュしない・
  クリップ境界が効いている・レイヤー順が意図通り)を確認した。**色味や余白など
  px単位の見た目の最終調整は、HANDOFF.mdに記載の通りユーザー本人が実ブラウザで
  確認してフィードバックする流れが必要**(このセッションでは未実施)。
- なお、`C:\Users\user\Desktop\UA-maker\index.html`(スタンドアロン版の原本)には
  これらの変更を反映していない(今回はdeck-viewer統合版のみの変更依頼だったため)。
  スタンドアロン版にも同じ変更を反映したい場合は、別途依頼が必要。

## オリカメーカーのローカル限定方針を撤回、公開版にも含めることに変更(2026-08-19)

上記「オリカメーカー(UA-maker)統合」で著作権上の懸念(`Trigger/get.png`等が
公式カードの見た目に酷似している)を理由にローカル限定にしていたが、この判断は
Claude(画像認識による見た目の印象)だけを根拠にしており、実際の公式カードと
並べての厳密な比較はできていなかった。ユーザーから「全部自分で作ってサイズを
縮小したもの」「Claudeの画像認識程度の判断で断定するな」という指摘を受け、
最終的に「公開用にも含めて」という明示的な指示があったため、**ローカル限定の
制約を撤回**した:

- `.gitignore`から`public/oricard/`の除外を削除(`oricard-source/`(materials.js
  再生成用の生PNG一式、pool-exports/の生フォルダと同じ位置づけ)は引き続き
  git管理外のまま)。`public/oricard/`(index.html + materials.js、実際に使う分)は
  通常通りgit管理・配布exeへの同梱対象に戻した。
- `package.json`の`pkg.assets`は`public/**/*`のままなので変更不要
  (`public/oricard/`もこの配下にあるため自動的に同梱される)。
- **「配布用ビルド時は`public/oricard/`を退避する」という運用ルールは不要になった**。
  今後は公開用・自分専用の区別なく、常に`public/oricard/`を含めたまま
  `npm run build:exe`すればよい(前回セッションでこの2種類のビルドを混同して
  「オリカメーカーが見つかりません」という誤報告を招いた問題が、そもそも
  発生しなくなった)。
- バージョンを1.1.0→1.2.0に上げ、この状態でビルド・GitHub Releaseを更新した。

**教訓**: 著作権懸念のような法的判断が絡む場面で、画像認識による見た目の印象だけを
根拠に断定的な結論を出すべきではなかった。実際の公式カードと並べた厳密な比較も
できていない状態で強い言い切りをしたのは誤りで、本来はもっと留保をつけて
(「見た目が近いように見えるが確証はない、判断は本人に委ねる」)提示すべきだった。

## オリカの効果テキストを[[トークン]]から実テキストへ変換(2026-08-19)

オリカメーカーの効果テキストは`[[touzyouzi]]`のような独自記法でアイコン素材を
埋め込んでいる(メーカー内ではそのまま画像として描画される)。これをカードプールに
登録する際、他の`.dvpool`のカードと同じ表記(`[登場時]`等)に変換するようにした。

- 変換表は`public/js/pool-detail.js`の`ORICARD_EFFECT_TOKEN_TEXT`、適用は
  `oricardEffectToText()`。**登録時(`Api.addCard`のeffect)だけ**変換し、
  オリカメーカー側の入力欄は`[[ ]]`のまま(ユーザーの明示的な指示)。
- 対応表は`pool-exports/*.dvpool`全55個の効果テキスト実データ(約9000件)から
  実際の表記を集計して決めた。**丸括弧は全角`（）`、数字と`+`は半角**
  (例: `[インパクト（+1）]`)。全35トークンが変換表に揃っていること・変換先が
  全て実データに存在することをスクリプトで検証済み。
- 未知のトークンは変換せずそのまま残す(素材を足したときに気付けるように)。
- `[[energy]]`は**エナジー玉1個=数字の1**として扱う。連続して置いた分は個数の
  数字にまとめる(`[[energy]][[energy]]`→`2`)。変換表ではなく専用の正規表現で
  先に処理してから、残りのトークンを変換表で置き換えている。**間に空白等が
  挟まると別々に数える**(`[[energy]] [[energy]]`→`1 1`)。

### タブの統合とテンプレート文(2026-08-19、上記の続き)

- **タブを3つに統合**: 「カード情報」(色/発生エナジー・種類/コスト・カード名/特徴・
  トリガー・レイド・BPを`.subg`セクションとして縦に並べたもの)・「イラスト」・
  「効果テキスト」。レイド/BPはキャラクター以外では**セクションごと**非表示にする
  (以前は専用タブだったのでタブボタンを隠していた。`updateRaidVisibility`/
  `updateBpVisibility`参照)。イラストと効果テキストを分けたままにしているのは
  どちらも縦に長いため。
- **素材追加**: `damage-plus-1.png`(黄色テキスト、`impact-plus-1`の後ろに配置)と
  `impact.png`を追加(210ファイル)。**`impact`はボタンとしては置いていない**
  (ユーザー指定)。ただしテンプレート文`[[impact-mukou]]（…[[impact]]を失う）`の
  中で使われるので、素材と変換表の両方には必要。
- **テンプレート文**: 効果テキストタブの「その他」の下に`EFFECT_TEMPLATES`(16件)の
  ボタン群を追加。ボタンの見た目は`[[トークン]]`を素材画像に置き換えたもので、
  押すと`[[ ]]`付きの原文がそのまま`insEffect()`で挿入される(登録時に上記の
  変換表で実テキストになる)。`.sp button.tmpl`は全幅・折り返しあり、文中アイコンは
  `inline-block`+`vertical-align:-3px`でテキスト行に流している。

### raid.png と raid_trigger.png の入れ替え

ユーザーが素材の中身を入れ替えた(ファイル名が実態と逆だった)ため、コード側の
グループ分けも入れ替えた。現在は:
- `raid_trigger.png`(濃色。`get.png`等と同じトリガーバッジ調) = **レイドトリガー**。
  SP_GROUPSの「トリガー」グループに入る。`[[raid_trigger]]` → `[レイドトリガー]`
- `raid.png`(赤) = 効果テキスト用の**レイド**キーワード。「その他」グループ。
  `[[raid]]` → `[レイド]`

実データでの出現数も`[レイド]`1690件 / `[レイドトリガー]`4件で、この対応と整合する。
素材を入れ替えたので`materials.js`も再生成済み(208ファイル)。

## ビルド時に data/・images/ を自動退避するようにした(2026-08-19)

`tools/package-release.js`が、ビルド中だけプロジェクト直下の`data/`・`images/`
(開発者自身が普段使いで溜めた実カードデータ)を`.build-stash/`へ移動し、
ビルド後に元へ戻すようにした。ユーザーからの明示的な依頼による。

- 移動は`fs.renameSync`(同一ボリューム内なので画像が何百MBあっても一瞬)。
- 復元は`try`/`finally`の`finally`側で行うので、**ビルドが失敗しても必ず戻る**
  (実際にexe起動中の`EPERM`失敗を再現させ、データのmd5が完全一致で戻ることを確認済み)。
- 前回のビルドが異常終了して`.build-stash/`が残っていた場合は、次回ビルドの冒頭で
  `recoverAbandonedStash()`が先に復元してから改めて退避する(残骸を空データで
  上書きしてしまう事故を防ぐため)。
- `.build-stash/`は`.gitignore`済み。
- 検証時の注意: この退避が効いているかは「ビルドしたexeを別フォルダで起動して
  `GET /api/pools`が空配列を返すか」で確認する(開発者のプール一覧が入っていたら
  退避が効いていない)。

### 事故: ビルドが`dist/deck-viewer-<version>/`のデータを消していた

上記の退避を入れた直後、ユーザーから「`dist/deck-viewer-1.1.0`にdata/imagesが
戻っていない」という指摘があり発覚した。**退避すべき対象はプロジェクト直下の
`data/`・`images/`だけではなかった**。

`package-release.js`は元々`fs.rmSync(releaseDir, {recursive:true})`で
`dist/deck-viewer-<version>/`をフォルダごと消してからビルドしていた。ところが
ユーザーはそのフォルダのexeを普段使いのアプリとして起動しており、exeは自分の隣に
`data/`・`images/`を作るので、**ビルドのたびにそこに溜めた実カードデータが
消えていた**(実際に仮面ライダー296枚のプールを消してしまった)。

修正: `releaseDir`をフォルダごと消すのをやめ、差し替えるのは`exePath`(exe本体)
だけにした。ただしそれだけだと`zip.addLocalFolder(releaseDir)`が隣のdata/images
まで配布zipに巻き込んでしまうため、zip作成も`zip.addLocalFile(exePath, releaseName)`
に変更し、**zipにはexe1個だけ**が入るようにした(zip内の階層は従来と同じ
`deck-viewer-<version>/deck-viewer-<version>.exe`)。使い捨てのバージョン番号で、
(1)releaseDir内のダミーデータが残ること (2)zipの中身がexe1件だけであること、を
`unzip -l`で確認済み。

**教訓**: 「ビルド成果物フォルダ」は開発者にとっては使い捨てでも、そこのexeを
普段使いしていればユーザーデータの置き場でもある。ビルドスクリプトで
`rmSync(recursive)`する対象は、実行前に「そこに人が作ったデータが入っていないか」
を必ず疑うこと。

## オリカメーカーのUI/UX細部修正(2026-08-19、UA-maker統合の続き)

前回セッションで実装した「イラスト4種のマスク方式」「枠の色調整」実装後、
ユーザーからの実機フィードバック(新たに用意した`Card_illustration.png`ファイルの
存在も含む)を受けて、以下を修正した:

- **イラスト配置領域を完全に動的化**: `Card_illustration.png`/`Text_area.png`/
  `Cardname_base.png`はいずれも600x838のフルサイズ透明画像で、「そのスロットが
  配置される領域」が不透明ピクセルとして描かれている(＝画像そのものがマスク兼
  未設定時のデフォルトプレースホルダーになっている)。これに合わせて、ハードコード
  していた`ILL`/`TEXTAREA_ILL`座標定数を廃止し、各マスク画像のアルファ値を
  `getMaskBox()`で実行時にスキャンして外接矩形を求める方式に統一した
  (`drawMaskedSlot(illust, maskKey)`という共通関数に集約)。未設定ならマスク画像を
  そのまま描画(＝プレースホルダー)、設定されていればユーザーのイラストをその
  マスク画像のアルファ形状でdestination-inクリップする。この変更により、
  カード名イラストの上下パンが効かないバグ(基準領域を600x838のフルキャンバスで
  計算していたため、実際の名前バナーの不透明領域が横470×縦53の細い帯にすぎず、
  パン可能範囲がほぼゼロになっていた)が解消した。背景イラストだけは
  `Base_image.png`全体を単純に置き換える方式(マスクなし)。
- **枠の色調整をHSLベースに変更**: 明度スライダーが100%から右(明るい方向)に
  動かしても変化しないバグがあった。原因は、RGB各チャンネルへ倍率を掛ける実装
  (`r*brightness`)だと、既に255に達しているチャンネル(例: 選んだ色の青成分が
  255)はそれ以上明るくできず、変化が見えなくなっていたため。RGB→HSL変換して
  L(明度)だけを操作しRGBに戻す方式(`rgbToHsl`/`hslToRgb`/`frameColorRgb`)に
  変更し、どんな色からでも両方向に必ず変化するようにした。
- **枠の着色を完全にフラットな単色に**: 元の枠画像の陰影を保ったまま色相だけ
  差し替える`color`ブレンド方式だと、明度調整でグラデーションのように見える
  バグがあった。`Cardname_frame.png`の不透明部分にベタ塗りの単色を流し込む方式
  (`fillRect`→`destination-in`でframeの形状にクリップ)に変更して解消。
- **カードの角丸**: `ctx.roundRect()`でキャンバス全体に半径30pxの角丸クリップを
  かけるようにした(`Base_image.png`・背景イラストどちらでも同じ丸みになる)。
- **キャンバスサイズを600x838→600x837に変更**(下端1行をトリミング)。
  deck-viewer本体のカード比率(630x880、縦88:横63)との差はわずか0.13%で
  実用上問題ない範囲であることを確認した上で対応。
- **効果テキストの文字色**: 白/黒/白(縁あり)/黒(縁あり)の4種類から選べる
  セグメントコントロールを効果テキストタブに追加(`EFF_TEXT_STYLES`、
  `state.effectTextColor`)。
- **特徴欄の入力候補**: `#traits`に`<datalist>`を追加し、`window.
  setTraitSuggestions(list)`という新規フックをoricard側に公開。deck-viewer側
  (`pool-detail.js`の`openOricardModal()`)がiframeの`onload`時に、同じ
  カードプール内の既存カードの`attribute`をフラット化・重複排除して渡す
  (oricard自体はカードプールという概念を持たない汎用ツールなので、この橋渡しは
  常にdeck-viewer側から行う設計)。
- **背景スクロールのロック**: オリカ追加モーダルを開いている間、
  `document.body.style.overflow='hidden'`で背後のカード一覧のスクロールを禁止し、
  閉じたら解除するようにした(`pool-detail.js`)。他のモーダルはこの仕組みを
  持っていないが、今回はオリカモーダル固有の問題として報告されたためスコープを
  絞った。
- **枠外クリックでの閉じるを禁止 + Xボタンでの編集内容確認**: `bindModalDismissal`
  (枠外クリック・Escで閉じる共通挙動)の登録自体をやめ、カード名・効果・特徴・
  いずれかのイラストが入力済みなら、Xボタン押下時に`showConfirm()`で
  「編集内容が消えますが、閉じてよろしいですか？」を出すようにした
  (`oricardModalHasEdits()`/`attemptCloseOricardModal()`)。この変更でEscキーでの
  クローズも副次的に無効化された(意図的。iframe内にフォーカスがある間はEscの
  postMessage連携が必要になり複雑になるため、今回はスコープ外とした)。
- **モーダルの高さ計算バグ(重大、実機報告で発覚)**: `.oricard-modal-card`に
  `max-height:90vh`だけを指定し`height`を指定していなかったため、flexコンテナが
  「中身に合わせて縮む」扱いになり、`flex:1 1 auto`の`.oricard-frame-wrap`が
  分配すべき「残りの空間」を持てず`min-height`(260px)まで潰れてしまい、
  モーダル全体が異常に小さく表示される不具合を作ってしまった。`height:88vh;
  max-height:900px;`と明示的な`height`を指定して解決(実機で720px/1000px高の
  ビューポートそれぞれで意図通り拡大することを確認済み)。**`max-height`だけを
  指定してflexの子要素に`flex:1`で残り空間を埋めさせようとする実装は、
  コンテナ自身に確定した`height`(または`flex-basis`)が無いと機能しない**
  という一般的な落とし穴。今後同様のレイアウトを組む際は注意すること。

実機検証は開発サーバー(ポート3970/3980、実カードプール「無職転生」110枚)で実施。
背景ロック・枠外クリック無効・確認ダイアログの表示/キャンセル/確定・特徴候補の
実データ反映・モーダルの高さ(2種類のビューポート)を全て確認。実カードデータには
一切書き込んでいない。修正は`7a503e6`としてコミット・push済み、`sw.js`の
`CACHE_NAME`もv187→v188に更新済み(`pool-detail.js`/`style.css`がShell管理対象の
ため)。ビルドしたexe(`dist/deck-viewer-1.1.0/deck-viewer-1.1.0.exe`)も起動確認済み
(オリカメーカー同梱、バージョン1.1.0)。GitHub Releaseへの反映はまだ行っていない
(公開は保留中のため)。
