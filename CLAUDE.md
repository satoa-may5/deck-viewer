# deck-viewer プロジェクト情報 (Claude向け)

このファイルは複数PC/複数セッション間でプロジェクトの文脈を引き継ぐためのもの。
git管理下に置き、どのPCでこのリポジトリを開いても同じ前提でやり取りできるようにする。
(Claude Codeのローカルメモリ機能は端末ごとに独立していて他PCに引き継がれないため、
永続化したい決定事項・方針は全てこのファイルに書く)

## プロジェクトの目的

ローカルにあるカード画像を使って、デッキのカード一覧を「Bandai TCG+」アプリの
デッキ一覧画面のような見た目(グリッド表示、右上に黒丸で枚数バッジ、下にカードID)
で表示するアプリ。対象イメージは元々
`Screenshot_20260721_165706_Bandai_TCG_Plus.jpg` (リポジトリ直下にあった参考画像)。

## 現状 (MVP実装済み)

- ビルドツール・サーバー不要、`index.html` を直接ブラウザで開くだけで動く構成
- カード画像: `images/{カードID}.拡張子` という命名規則
- デッキ定義: `decks/*.js` に `const DECK = { name, cards: [{id, count}] }` 形式で記述
- 画像が無いカードはプレースホルダー表示(斜線+ID文字)でレイアウトを崩さない
- カード画像自体は `.gitignore` で除外(著作権・サイズの都合)。`images/.gitkeep` のみ管理下

## 決定事項・方針

- デッキの入力方法は「まずファイル入力(`decks/*.js`)、後でクリック操作のUIを追加」の2段階アプローチで進める方針(ユーザーが選択済み)
- カード画像は用意済みのものが無く、これから収集する。ファイル名とカードIDを最初から一致させる運用にする(マッピング処理を作らずに済むため)
- git操作(init/commit/push等)はユーザー自身が手を動かしたいとのことなので、Claudeは実行せず手順を案内するだけにする

## 次にやること候補

- クリックでカードを選んで枚数を増減できるUI(画像一覧ピッカー)
- 複数デッキの切り替えUI/一覧
- 画像収集後の実データでの見た目調整(アスペクト比・バッジ位置など)
