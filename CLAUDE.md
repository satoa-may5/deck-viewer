# deck-viewer プロジェクト情報 (Claude向け)

このファイルは複数PC/複数セッション間でプロジェクトの文脈を引き継ぐためのもの。
git管理下に置き、どのPCでこのリポジトリを開いても同じ前提でやり取りできるようにする。
(Claude Codeのローカルメモリ機能は端末ごとに独立していて他PCに引き継がれないため、
永続化したい決定事項・方針は全てこのファイルに書く)

## プロジェクトの目的

ローカルにあるカード画像を使って、Bandai TCG+ アプリのデッキ一覧画面のような見た目
(グリッド表示、右上に黒丸で枚数バッジ、下にカードID)でデッキのカード一覧を表示する。
さらに、以下を追加で実現したい:

1. 「カードを追加」画面: 画像をアップロード→トリミング(縦88:横63固定)→カード名・
   カード番号(ID)・必要エナジーなどのメタデータ入力→保存、で手持ちカードを登録していく
2. Shadowverseのデッキ作成画面のような、横画面で上にデッキ・下に手持ちカード一覧を
   並べ、カードをクリック/スワイプしてデッキに追加していくビルダー画面
3. ブラウザ・デスクトップアプリ・モバイルアプリ、どの形でも「ポチポチ操作するだけ」で
   使えるようにしたい(PWAとしてインストール可能にする方針)

## アーキテクチャ(2026-07-21時点、大きく刷新済み)

当初は無サーバーの静的HTMLで作っていたが、カード画像・メタデータを**自動保存**する
機能が必要になったため、Node.js + Express のローカルサーバー構成に切り替えた
(理由: file://からの直接書き込みはブラウザのセキュリティ制限で困難。File System
Access APIはChromium限定かつfile://で不安定。ユーザーは「ブラウザ/デスクトップ/
モバイルどれでもポチポチで使いたい、時間がかかってもよい」と回答したため、
サーバー経由の自動保存 + PWA化を選択した)。

さらに同日中に「カードプール」概念を追加。カード登録はプール単位で行い、デッキは
1つ以上のカードプールを「参照」して手持ち一覧を組み立てる形に変更した。

同日中にさらにUIを全面刷新(グラデーションアクセント・角丸・影を使ったモダンな
デザインシステムに変更)し、続けてホーム画面を「デッキ」「カードプール」の2タブ構成に
再編。両タブとも「＋ 作る」という点線区切りのゴースト行が一覧の先頭に常設され、
デッキ/カードプールそれぞれに削除・並び替え(▲▼ボタン)を追加した。これに伴い
`pools.html`/`js/pools.js`は廃止し、機能を`index.html`/`js/home.js`に統合した。タブは
その後さらに「アクティブなタブが下のコンテンツと同じ背景色でシームレスに繋がり、
非アクティブなタブは角が欠けた小さいチップになる」フォルダタブ風の見た目に調整し、
2つのタブは常に画面幅ちょうど半分ずつで固定(伸縮しない)にした。

続けて、カードプール個別ページ(`pool-detail.html`)を新設し、カードプール一覧の行を
大幅刷新: 「カードを追加」リンクと⋮メニューを廃止し、＋(カード追加)/★(お気に入り)/
✎(リネーム)/🗑(削除)の4つのアイコンボタンと、左端に三本線のドラッグハンドルを配置。
▲▼ボタンではなくポインタードラッグでの並び替えに変更した(`js/sortable.js`に
共通ロジックを切り出し、カードプール一覧・カードプール個別ページ内のカード一覧の
両方で使い回している)。カードプールの「＋ 作る」ゴースト行はその場でのインライン
作成ではなく、クリック時にプールを仮作成してから`pool-detail.html`に遷移する方式に
変更した。デッキ側(▲▼ボタン+⋮メニュー)は今回は変更していない。

`pool-detail.html`は上部にカードプール名の入力欄(blur/Enterで自動保存)、下に
そのプールのカード一覧(ドラッグ並び替え・✎編集[名前/コストのその場編集]・🗑削除)、
右下にフローティングの＋ボタン(`add-card.html?poolId=`へ遷移)を持つ。`add-card.html`は
`?poolId=`が付いている場合、プール選択ステップを飛ばして画像選択から始まり、
「一覧に戻る」も`pool-detail.html`に戻るようになる。

```
deck-viewer/
├── server.js              Express本体。静的配信 + /api/cards, /api/pools, /api/decks
├── package.json            依存: express, multer
├── data/
│   ├── cards.json          登録済みカードのDB(id, name, cost, poolId, imageExt, order)
│   ├── cardpools.json      カードプールのDB(id, name, favorite, createdAt)。配列の並び順=表示順
│   └── decks/{id}.json     デッキごとの保存ファイル(name, poolIds:[], cards:[{cardId,count}], order)
├── images/                 カード画像本体。.gitignoreで除外(著作権・サイズの都合)
│   └── union_arena_cards/  ユーザーが用意したテスト用カード画像(311枚、Union Arena)。
│                           開発中の動作確認に自由に使ってよいとユーザーから許可済み
└── public/                 フロントエンド(静的ファイルとしてExpressが配信)
    ├── index.html          ホーム: 「デッキ」「カードプール」タブ(フォルダタブ風・幅半分固定)。
    │                       各タブにゴースト作成行+一覧(削除・ドラッグ並び替え[プール]/
    │                       ▲▼[デッキ]・リネーム・複製[デッキのみ]・お気に入り[プールのみ])
    ├── pool-detail.html/js  カードプール個別ページ(名前編集+カード一覧+カード追加FAB)
    ├── add-card.html/js     カード追加(ステップ表示付き: プール選択→アップロード→トリミング→メタデータ入力。
    │                       `?poolId=`指定時はプール選択を省略)
    ├── builder.html/js      Shadowverse風デッキビルダー(参照プールをピル型トグルで複数選択→上:デッキ 下:手持ち一覧)
    ├── deck-view.html/js    デッキの読み取り専用グリッド表示(元々の目標画面)
    ├── js/api.js            サーバーAPIへのfetchラッパー
    ├── js/card-render.js    カード1枚分のDOM生成(グリッド/ビルダー共通)
    ├── js/crop.js           パン/ズーム式トリミングツール(CropToolクラス)
    ├── js/sortable.js       ポインタードラッグによる並び替え共通ロジック(makeSortable)
    ├── manifest.webmanifest, sw.js, icons/  PWA化(ホーム画面インストール用)
    └── css/style.css        デザインシステム(CSS変数でaccent/shadow/radius等を定義)
```

## データ/API仕様

- カード画像ファイル名: `images/{カードID}.{jpg|png|webp}`(カードIDをそのままファイル名に使う)
- カードの必要エナジー等の情報は `data/cards.json` に集約(画像とは別管理)。各カードは
  必ずいずれか1つの`poolId`(カードプールID)に属する
- カードプールは`data/cardpools.json`で管理。カードプール自体に画像やメタデータは
  持たず、単なる「カードのグループ分け」(例: 弾ごと、購入ロットごとなど)。**配列の
  並び順がそのまま表示順**(並び替えは配列を並べ替えて書き戻すだけ)
- デッキは`poolIds`(参照するカードプールIDの配列)と`order`(表示順を表す数値。新規作成時
  は既存の最大値+1を自動採番)を持つ。ビルダーの「手持ちカード」一覧は、選択中のプールに
  属するカードだけをフィルタして表示する
- カードのアスペクト比は縦88:横63固定(CSSで `aspect-ratio: 63 / 88`、トリミング出力も630x880)
- API: `GET/POST/PATCH/DELETE /api/pools(/:id)`(PATCHは`name`/`favorite`を部分更新可),
  `POST /api/pools/reorder`(`{order:[id,...]}`), `GET/POST/PATCH/DELETE /api/cards(/:id)`
  (GETは`?poolId=`でフィルタ可、PATCHは`name`/`cost`のみ更新、DELETEは画像ファイルも削除)、
  `POST /api/cards/reorder`(`{order:[id,...]}`。渡されたidだけの`order`フィールドを
  0始まりで振り直す。全カード分を含める必要はなく、通常は同一プール内のカードだけを渡す)、
  `GET/POST/DELETE /api/decks(/:id)`, `POST /api/decks/reorder`(`{order:[id,...]}`)
  (詳細はserver.js参照)
- デッキのリネーム・複製は専用APIを設けず、既存の`POST /api/decks`(id指定でupsert)を
  フロント側で使い回している(リネーム=同じid+新name+既存cardsで再POST、
  複製=id省略+同じcards/poolIdsで新規POST。`order`はサーバー側が既存デッキなら維持、
  新規なら自動採番するのでクライアントは意識しなくてよい)
- **カードプールの削除はカスケード削除**: `DELETE /api/pools/:id`はプール自体だけでなく、
  そのプールに属する全カードのレコードと画像ファイルも削除する。デッキ側の`cards`に
  残った参照は「見つからないカード」として既存のプレースホルダー表示(IDだけ表示)で
  グレースフルに扱われる(孤立を許容する設計)

## 決定事項・方針

- **2026-07-22時点の最新方針**: ユーザーから「コミットプッシュはこれから勝手にしていいよ
  (一応報告して)」と明示的な標準許可をもらった。これ以降は、都度確認を挟まずcommit/push
  してよい。ただし実行した際は必ずその旨をユーザーに報告すること(何をコミットしたか
  一言添える)。それ以前の「基本ユーザー自身が行う」という方針は本プロジェクトについては
  上書き済み。**同日中に追加の指示**: 「コミットプッシュはある程度変更が溜まってから
  でいい」とのこと。細かい修正1つ1つでコミットせず、ある程度まとまった単位(1つの
  依頼・1つの機能追加が完了した区切りなど)でまとめてcommit/pushする。
- カード画像自体はgit管理外(`.gitignore`で`images/*`除外)。`data/cards.json`と
  `data/decks/*.json`は通常のテキストなので**git管理に含める**(他PCへの引き継ぎ・
  同期はこれらのJSONについてはgit経由で行える。画像だけは各PCで別途用意が必要)
- 命名規則: カードIDとファイル名を最初から一致させる運用(マッピング処理を作らずに済む)

## はまりどころ(実装時に踏んだバグ、再発防止用)

- **Service WorkerのシェルキャッシュがJS/CSSの変更を握り潰す**: `sw.js`は
  `CACHE_NAME`(例: `deck-viewer-shell-v2`)をキーにshellファイル(html/css/js)を
  cache-firstで返す。`public/`配下のJS/CSS/HTMLを変更したら**必ず`sw.js`の
  `CACHE_NAME`をインクリメントし、`SHELL_FILES`に新規ファイルがあれば追加すること**。
  やらないと、ブラウザは古いsw.jsのバイト列のままだと判定して再インストールせず、
  変更が永久に反映されない(実際に`Api.createPool is not a function`という形で
  発生した)。開発中に動作確認する際は、コンソールで
  `navigator.serviceWorker.getRegistrations().then(rs=>rs.forEach(r=>r.unregister()))`
  `caches.keys().then(ks=>ks.forEach(k=>caches.delete(k)))` を実行してから
  リロードすると確実に最新化できる。
- **`hidden`属性とCSSの優先順位**: `.pool-chip { display: flex; }` のような
  クラスセレクタは、UAスタイルシートの `[hidden] { display: none }` と同じ詳細度
  (0-1-0)だが、author stylesheet(自分の`style.css`)の方がUAスタイルシートより
  優先されるため、`hidden`属性を上書きしてしまい要素が消えなくなるバグがあった。
  対策として`style.css`冒頭に `[hidden] { display: none !important; }` を追加済み。
  今後 `hidden` 属性で出し分けする要素に無条件の `display` ルールを書くときは注意。
- **共通コンポーネントのコールバックをハードコードしない**: `home.js`の`startRename()`は
  デッキ行/カードプール行の両方から共有される「その場でリネーム」UIだが、初期実装では
  キャンセルボタンのハンドラを`renderDecks`に決め打ちしていたため、カードプール側で
  リネームをキャンセルしても画面が更新されず固まって見えるバグがあった。共有UIヘルパーを
  複数の呼び出し元で使う場合、再描画関数などのコールバックは必ず引数として受け取り、
  呼び出し元ごとに正しい関数を渡すこと。
- **ドラッグ中の要素にCSS transitionが付いていると`getBoundingClientRect()`が
  古い値を返す**: `js/sortable.js`(`makeSortable`)はドラッグ中の要素に
  `style.transform = translateY(...)`を設定し、直後に`getBoundingClientRect()`で
  現在位置を読んで並び替え判定をしているが、`.deck-row`に`transition: transform 0.2s`
  が付いていたため、transformを設定した直後(同一の同期処理内)に読み取ると
  アニメーション開始前の**古い位置**が返ってきてしまい、ドラッグの並び替え判定が
  一切発動しないバグがあった。対策として`.dragging`クラスに
  `transition: none !important;`を追加し、ドラッグ中は該当要素のtransitionを
  無効化して即座に新しい位置が反映されるようにした。今後、transformで要素を動かして
  すぐに`getBoundingClientRect()`を読む処理を書く場合、その要素(または祖先)に
  transformのtransitionが付いていないか必ず確認すること。
- **ドラッグの上下限クランプと入れ替え判定の境界条件**: `js/sortable.js`にドラッグ範囲を
  リストの先頭〜末尾内に収めるクランプ処理を追加した際、クランプ後の位置が隣接要素の
  中央と**ちょうど一致**するケース(要素が2個しかない時にリストの端まで一気にドラッグする
  場合など)で、入れ替え判定が厳密不等号(`<`/`>`)だったため境界上で入れ替えが
  発火しないバグがあった。`<=`/`>=`に変更して解決済み。座標比較で「ちょうど一致」が
  起こりうる場面(クランプ後の値を使う場合など)では厳密不等号を避けること。

## 現状 (2026-07-22時点)

同一PC上で以下を一通りブラウザ操作で実機確認済み:

- ホーム画面(index.html): 「デッキ」「カードプール」タブ切り替え、ゴースト作成行
  (デッキ側はbuilder.htmlへの遷移リンク、カードプール側はその場でテキスト入力に変わり
  Enter/ボタンで作成)、⋮メニューからのリネーム/複製[デッキのみ]/削除、▲▼による並び替え
  → いずれも動作確認済み(ライト/ダーク/モバイル幅375pxすべて確認)
- カードプールの削除はカスケード削除(プール内カード・画像も一緒に消える)であることを
  確認。削除確認ダイアログにカード枚数を含めて警告する
- 「カードを追加」画面: 1→2→3→4のステップインジケーター付きで、プール選択→ファイル
  選択→トリミング→メタデータ入力→保存の流れを実カード画像(`images/union_arena_cards/`)
  で確認
- ビルダー画面: カードプールをピル型トグルボタンで複数選択→手持ち一覧に反映→タップで
  デッキに追加→保存、を確認
- デッキ表示画面(deck-view.html): グリッド+黒丸バッジ+カード名表示、目標のBandai TCG+風
  の見た目になっていることを確認
- コンソールエラーなし

同日中に続けて、カードプール個別ページ・カード編集/削除・ドラッグ並び替えも実機確認済み:

- ホーム画面のカードプール行: ＋(カードを追加)/★(お気に入り)/✎(リネーム)/🗑(削除、
  カスケード削除の警告文言つき)の4アイコンボタンと、左端のドラッグハンドルでの
  並び替え(ポインタードラッグ、実際にマウスドラッグで並び替わることを確認)
- カードプール行クリック→`pool-detail.html`遷移、名前欄の自動保存(blur)、
  右下＋ボタン→`add-card.html?poolId=`遷移(プール選択ステップを飛ばして画像選択から
  開始することを確認)、保存後「一覧に戻る」で`pool-detail.html`に戻ることを確認
- `pool-detail.html`のカード一覧: ドラッグ並び替え、✎編集(名前・コストのその場編集→
  `PATCH /api/cards/:id`で保存)、🗑削除(`DELETE /api/cards/:id`で画像ファイルも
  削除されることを確認)
- ライト/ダーク/モバイル幅375pxいずれも確認、コンソールエラーなし

同日中にさらに細かい修正: ドラッグがリストの先頭/末尾を超えて際限なく動いてしまう
バグを修正(コンテナの上端/下端でクランプ)、アイコンボタンの「＋」がリンク要素
(`<a>`)だったためUAデフォルトの下線が出ていたのを`.icon-btn`に`text-decoration: none`
を追加して解消、各画面の「一覧に戻る」等の戻るボタンから「←」矢印を削除、ホーム画面の
選択中タブ(デッキ/カードプール)を`localStorage`に保存し、他画面から「一覧に戻る」で
戻ってきた際も直前のタブを維持するようにした。

検証で作成したテスト用デッキ・カードプール・カード(「test2」「デザインテストプール」
「テストカードA/B」など自分で作成したもの)は確認後に削除済み。ただしユーザー自身が
作成したと思われる「test」「a」という名前のカードプールが`data/cardpools.json`に
残っており、これらは触っていない(セッション開始時点で既に存在していたため、
テストデータと断定せず残した)。

残っている未着手項目:

1. モバイルからのLANアクセス(`http://<PCのLAN IP>:3000`)は未検証。LAN上のhttpアクセスは
   「secure context」にならないため、Service WorkerによるPWAインストールが
   モバイル側でうまく機能しない可能性がある(要検証)
2. デッキの並び替えは▲▼ボタン方式のまま(カードプールのみドラッグ&ドロップ化した)。
   デッキ側もドラッグにしたい場合は`js/sortable.js`の`makeSortable`を流用すれば
   同様に対応可能
3. カードプールの「お気に入り」フラグは保存・表示のみで、並び替えや絞り込みへの
   反映はまだ実装していない(必要になったら要相談)

## 今後の予定

- モバイル実機でのLANアクセス・PWAインストールの検証
- 必要であればデッキ側の並び替えもドラッグ化、お気に入りの活用(絞り込み等)
